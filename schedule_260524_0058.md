# AutoDrama 角色抽取三层收敛规划

时间：2026-05-24 00:58  
范围：`role_extract` 重要性等级收敛、主要角色/功能角色分节点抽取、背景实体不进入角色资产链  
原则：角色资产链只处理“主要角色”和“功能角色”；背景不进 `roles`。主要角色和功能角色必须分节点抽取、分列表输入、分语义标准判断。

## 1. 背景问题

当前角色抽取仍沿用多级 `importance`：

```text
lead
main
supporting
minor
background
```

这个分级看起来细，但对后续资产链真正要回答的问题不够直接：

- 这个人物要不要进入完整角色资产链？
- 这个人物要不要进入轻量角色资产链？
- 这个人物是否只是背景氛围，不应进入角色系统？

因此，建议将原有多个重要性级别收敛为三层语义：

```text
primary      主要角色
functional   功能角色
ambient      背景存在 / 背景实体
```

其中只有前两类进入 `roles`：

```text
primary roles      -> 进入角色资产链
functional roles   -> 进入轻量角色资产链
ambient entities   -> 不进入 roles，后续可单独索引给场景/分镜使用
```

## 2. 三层定义

### 2.1 主要角色 primary

主要角色是对剧情结构、人物关系、情节推进具有持续影响的人。

满足任一强条件即可归为主要角色：

- 是主角、反派、核心盟友、核心师长、核心亲属、核心情感对象。
- 多集或多章节持续出现。
- 有明确人物目标、动机、秘密、成长线、阵营立场或关系变化。
- 与主角存在稳定关系，例如师徒、仇敌、同门、血亲、道侣、宿敌、上级、宗门核心成员。
- 后续镜头需要长期保持外观、声音、气质一致。

弱条件包括：

- 有名字或稳定称号。
- 多次与主角对话。
- 在关键事件中作出决定。
- 被多个角色反复提及。
- 对剧情结果产生长期后果。

玄幻语境示例：

```text
沈烬
云蘅
玄霄真人
赤焰魔君
白璃圣女
戒律堂首座
沈烬之母
青霄宗宗主
```

### 2.2 功能角色 functional

功能角色是短暂出现、没有长期人物线，但在某个场景中承担明确动作、对白、阻碍、引导、通报、战斗、交易、治疗等功能的人。

判断核心：

```text
如果删除这个人，会导致某个镜头动作、对白、信息传递或事件触发变得不清楚，那么这个人就是功能角色。
```

典型条件：

- 出现次数少，通常只在一集、一章或一个场景中出现。
- 没有长期人物弧线。
- 不承担主线人物关系。
- 但有明确独立动作或台词。
- 需要在分镜中被稳定引用。
- 可能需要基础外观，部分需要声音。

玄幻语境示例：

```text
山门守卫
传令剑侍
青衣药童
戒律堂执事
黑甲妖兵甲
妖市摊主
白发丹师
押阵魔修
灵舟舵手
试炼主持弟子
```

功能角色可以有名字，也可以无名。关键不是有没有名字，而是有没有短期场景功能。

### 2.3 背景存在 ambient

背景存在不是角色资产。它们只用于场景氛围和镜头背景。

满足以下条件的，不进入 `roles`：

- 没有单独动作。
- 没有单独台词。
- 不与主要角色发生明确互动。
- 不需要稳定外观。
- 只是营造环境、势力规模、场面气氛。

玄幻语境示例：

```text
围观修士
山门弟子们
魔兵队列
妖市行人
宴席宾客
试炼弟子人群
宗门长老席众人
低阶妖族
```

背景存在后续如有需要，可以单独进入 `ambient_entities.json`，供 layout、storyboard、ref frame、video prompt 使用，但不进入角色设计、声音、角色图、角色视频流程。

## 3. 节点拆分设计

建议将当前单一 `role_extract` 拆成两个真实抽取节点，再生成一个合并结果。

```text
role_extract_primary
  - 抽取主要角色
  - 循环
  - 只输出 primary

role_extract_functional
  - 抽取功能角色
  - 循环
  - 输入 primary 列表 + functional 列表
  - 只输出 functional

role_extract
  - 合并输出
  - 供下游 role_design 继续读取
```

推荐落盘：

```text
assets/json/nodes/role_extract_primary.json
assets/json/nodes/role_extract_functional.json
assets/json/nodes/role_extract.json
assets/json/roles/role_xxx.json
```

其中：

- `role_extract_primary.json` 保存主要角色原始抽取结果。
- `role_extract_functional.json` 保存功能角色原始抽取结果。
- `role_extract.json` 保存合并后的角色清单，保持下游入口清晰。
- per-role JSON 仍按当前角色状态落盘规则写入。

## 4. 节点 1：role_extract_primary

### 4.1 职责

从完整小说中抽取主要角色。

不抽：

- 短暂功能人物。
- 纯背景人群。
- 只服务于一个动作的小角色。

### 4.2 输入

```text
novel_full
existing_primary_roles: list[tuple[name, brief]]
episode_keys
visual_style_label
visual_style_prompt
```

`existing_primary_roles` 每项是：

```json
["沈烬", "被逐出宗门的少年剑修，在山门禁阵前寻找师父失踪线索。"]
```

### 4.3 循环规则

```text
最多 10 轮。
每轮输入完整 novel_full + 已抽取主要角色列表。
本轮只输出新增主要角色。
无新增则停止。
```

### 4.4 Prompt 核心要求

```text
从完整小说中抽取主要角色。不要抽取短暂功能人物和纯背景人群。

主要角色定义：
- 对主线、人物关系、阵营冲突或长期剧情有持续影响。
- 有持续目标、动机、秘密、成长线、关系变化。
- 多次出现或被多次提及。
- 后续需要保持外观、声音、气质一致。

不要输出：
- 只出现一次的守卫、药童、摊主、传令弟子、妖兵。
- 纯背景人群，如围观修士、众弟子、魔兵们。

本轮只输出“已抽取主要角色列表”中尚未包含的新增主要角色。
没有新增主要角色时，输出空 roles 数组。
role_tier 固定为 primary。
```

## 5. 节点 2：role_extract_functional

### 5.1 职责

在排除已抽取主要角色后，从完整小说中抽取无名或短暂出现但有明确场景功能的人物。

### 5.2 输入列表必须分开

功能角色抽取时，输入的已抽取角色列表必须分成两个性质不同的列表：

```text
primary_roles: 已抽取主要角色列表
functional_roles: 已抽取功能角色列表
```

不要混成一个列表。

原因：

- `primary_roles` 用于排除核心人物，防止重复抽取或降级。
- `functional_roles` 用于排除已发现的功能人物，防止重复。
- 分开后 prompt 可以明确“你的任务不是重新判断主要角色，而是在主要角色之外查漏功能角色”。
- 混合列表只解决去重，不解决主/功能边界。

输入示例：

```json
{
  "primary_roles": [
    ["沈烬", "被逐出宗门的少年剑修，在山门禁阵前寻找师父失踪线索。"],
    ["云蘅", "灵药峰弟子，暗中把禁阵残片交给沈烬。"]
  ],
  "functional_roles": [
    ["山门守卫", "守在青霄宗山门前的无名弟子，持令牌阻拦沈烬入山。"]
  ]
}
```

### 5.3 循环规则

```text
最多 10 轮，或更保守 5 轮。
每轮输入完整 novel_full + primary_roles + functional_roles。
本轮只输出新增功能角色。
无新增则停止。
```

### 5.4 功能角色判定标准

不满足主要角色条件，但满足任一条，归为功能角色：

- 有独立动作并推动当前场景：阻拦、盘问、开阵、引路、通报、递交、治疗、追杀、押送、交易。
- 有独立台词、喊话、传音、命令、警告。
- 与主要角色发生直接互动。
- 是镜头焦点的一部分，需要观众识别这个人正在做什么。
- 后续分镜需要引用这个具体身份。

必须抽取的示例：

```text
山门守卫：阻拦主角入山
传令剑侍：通报宗主召见
青衣药童：递药并提醒禁忌
妖市摊主：卖出关键符箓
黑甲妖兵甲：追杀并与主角交手
白发丹师：短暂救治主角
```

不能抽取的示例：

```text
围观修士
众弟子
魔兵们
妖市行人
宴席宾客
长老席众人
```

### 5.5 Prompt 核心要求

```text
你的任务不是重新抽取主要角色。
主要角色列表中的人物绝对不要作为功能角色重复输出。

你只需要在排除主要角色之后，查找仍然具有明确场景功能的短暂人物。

功能角色定义：
- 不承担长期人物线。
- 通常只服务于一个或少数场景。
- 但有独立动作、对白、交互、信息传递、阻碍、引导、战斗、救治等明确功能。

不要输出背景群体。
不要把主要角色换称谓后作为功能角色输出。
没有新增功能角色时，输出空 roles 数组。
role_tier 固定为 functional。
```

## 6. 主/功能边界判定顺序

模型和代码都应按以下顺序判断。

### 6.1 先判断是否主要角色

只要满足任一条，就归为主要角色：

- 有持续人物线：目标、动机、成长、秘密、阵营变化、关系变化。
- 与主角存在稳定强关系：师徒、亲属、同门、宿敌、道侣、主仆、盟友、核心敌人。
- 多集/多章持续出现，并多次影响剧情走向。
- 是关键势力的代表人物，后续反复出现或被反复提及。
- 需要长期保持声音、外观、服装、法器、气质一致。

如果满足主要角色条件，即使某场戏里只是在传话、递药、阻拦，也不要放入功能角色。

### 6.2 再判断是否功能角色

不满足主要角色，但有短期场景功能的人，归为功能角色。

典型短期功能：

```text
阻拦
盘问
开阵
引路
通报
递交
治疗
追杀
押送
交易
喊话
传音
命令
警告
```

### 6.3 最后判断是否背景存在

如果只是人群、队列、氛围、势力规模，不进 `roles`。

## 7. Schema 建议

建议替换旧 `importance`，不要继续用五级。

### 7.1 RoleExtractItem

建议结构：

```python
class RoleExtractItem(BaseModel):
    name: str
    role_tier: Literal["primary", "functional"]
    aliases: list[str] = Field(default_factory=list)
    episode_keys: list[str] = Field(default_factory=list)
    source_chapters: list[str] = Field(default_factory=list)
    brief: str | None = None
    appearance_notes: list[str] = Field(default_factory=list)
    has_dialogue: bool = False
    visual_reuse_required: bool = False
```

第一版可以暂时不加 `asset_policy`，因为 `role_tier` 已能初步决定：

```text
primary    -> full asset policy
functional -> basic asset policy
```

后续如需更细，再加：

```python
asset_policy: Literal["full", "basic", "appearance_only"] = "full"
```

### 7.2 AmbientEntityItem

背景存在不要进入 `RoleExtractItem`，建议使用独立 schema：

```python
class AmbientEntityItem(BaseModel):
    name: str
    entity_type: Literal["crowd", "faction_presence", "background_actor_group"]
    episode_keys: list[str] = Field(default_factory=list)
    description: str
    visual_notes: list[str] = Field(default_factory=list)
    usage: str | None = None
```

示例输出：

```json
{
  "name": "围观修士",
  "entity_type": "crowd",
  "episode_keys": ["episode_001"],
  "description": "聚在青霄宗山门外观望冲突的低阶修士人群。",
  "visual_notes": ["青灰道袍", "远景", "不可辨认具体面孔"],
  "usage": "作为山门冲突场景背景，不生成独立角色资产。"
}
```

## 8. 下游资产策略

如果主要角色和功能角色都进入 `roles`，下游必须知道二者区别，否则功能角色会成本过高。

推荐策略：

```text
primary:
- role_design 完整
- voice 生成
- appearance image 生成
- intro video 可生成

functional:
- role_design 简化
- 有台词才 voice
- 生成基础外观图
- 默认不生成 intro video
```

如果短期只改抽取，不改下游，功能角色也会进入完整资产链。短期可接受，长期不经济。

## 9. 背景实体抽取

背景不进 `roles`，因此不建议叫 `role_extract_background`。如果后续要做，建议叫：

```text
ambient_entity_extract
```

输出路径建议：

```text
assets/json/nodes/ambient_entity_extract.json
assets/json/assets/ambient_entities.json
```

服务对象：

```text
layout_design
storyboard_generation
ref_frame_generation
video_prompt
```

不服务于：

```text
role_design
role_voice_generation
role_appearance_generation
```

## 10. 推荐执行顺序

### Phase 1：Schema 与 prompt 收敛

目标：

- 新增或替换 `role_tier`。
- 明确 primary / functional / ambient 三层标准。
- 准备 `role_extract_primary.md` 与 `role_extract_functional.md`。

任务：

1. `RoleExtractItem` 增加 `role_tier`。
2. 决定是否立即删除旧 `importance`。
3. 新增 primary prompt。
4. 新增 functional prompt。
5. prompt 明确玄幻语境下的主要/功能/背景边界。

### Phase 2：主要角色抽取节点

目标：

- `role_extract_primary` 最多 10 轮循环抽取主要角色。

任务：

1. 新增 `RolePrimaryExtractNode`。
2. 输入完整 `novel_full`。
3. 输入 `existing_primary_roles`，每项为 `[name, brief]`。
4. 无新增则提前停止。
5. 输出 `assets/json/nodes/role_extract_primary.json`。

### Phase 3：功能角色抽取节点

目标：

- `role_extract_functional` 在主要角色之外查漏功能角色。

任务：

1. 新增 `RoleFunctionalExtractNode`。
2. 输入完整 `novel_full`。
3. 输入 `primary_roles`。
4. 输入 `existing_functional_roles`。
5. 无新增则提前停止。
6. 输出 `assets/json/nodes/role_extract_functional.json`。

### Phase 4：合并输出与 per-role JSON

目标：

- 合并 primary 与 functional，生成统一 `role_extract.json`。
- 下游 `role_design` 仍从合并结果读取。

任务：

1. 合并顺序稳定：primary 在前，functional 在后。
2. 去重：主要角色优先级高于功能角色。
3. 生成 `assets/json/nodes/role_extract.json`。
4. 生成或更新 `assets/json/roles/role_xxx.json`。
5. `state.json.roles` 仍只保存角色名到角色 JSON 路径引用。

### Phase 5：下游资产策略优化

目标：

- primary 完整设计。
- functional 轻量设计。

任务：

1. `role_design` prompt 根据 `role_tier` 区分设计深度。
2. `role_voice_generation` 对 functional 只在有台词时生成声音。
3. `role_appearance_generation` 对 functional 默认跳过 intro video。
4. smoke 覆盖功能角色不会触发过重资产生成。

### Phase 6：背景实体索引

目标：

- 抽取背景实体，但不进入角色系统。

任务：

1. 新增 `ambient_entity_extract`。
2. 输出 `ambient_entities.json`。
3. layout/storyboard/ref_frame/video prompt 可读取背景实体。
4. 确认背景实体不会进入 role_design、voice、appearance。

## 11. 验证计划

### 11.1 主要角色 smoke

输入玄幻文本：

```text
沈烬被逐出宗门，云蘅暗中相助，玄霄真人失踪，赤焰魔君设局。
山门守卫阻拦沈烬，围观修士聚在山门外。
```

预期：

```text
primary:
- 沈烬
- 云蘅
- 玄霄真人
- 赤焰魔君

不应出现在 primary:
- 山门守卫
- 围观修士
```

### 11.2 功能角色 smoke

输入同一文本，并传入 primary 列表。

预期：

```text
functional:
- 山门守卫

不应出现在 functional:
- 沈烬
- 云蘅
- 玄霄真人
- 赤焰魔君
- 围观修士
```

### 11.3 背景不进 roles smoke

输入包含：

```text
围观修士、山门弟子们、魔兵队列、妖市行人
```

预期：

```text
role_extract.json 不包含这些背景群体。
```

如果实现 ambient：

```text
ambient_entities.json 包含这些背景实体。
```

### 11.4 合并 smoke

验证：

- `role_extract_primary.json` 存在。
- `role_extract_functional.json` 存在。
- `role_extract.json` 存在。
- 合并顺序稳定：primary 在前，functional 在后。
- 同名冲突时 primary 优先。
- per-role JSON 生成正确。

### 11.5 下游资产策略 smoke

验证：

- primary 角色进入完整设计。
- functional 角色进入轻量设计。
- functional 默认不生成 intro video。
- functional 无台词时不生成 voice。

## 12. 最终目标形态

最终角色抽取结果应类似：

```json
{
  "roles": [
    {
      "name": "沈烬",
      "role_tier": "primary",
      "aliases": ["少宗主"],
      "episode_keys": ["episode_001"],
      "brief": "被逐出宗门的少年剑修，在山门禁阵前寻找师父失踪线索。"
    },
    {
      "name": "山门守卫",
      "role_tier": "functional",
      "aliases": ["守山弟子"],
      "episode_keys": ["episode_001"],
      "brief": "守在青霄宗山门前的无名弟子，持令牌阻拦沈烬入山。"
    }
  ]
}
```

背景实体不在 `roles` 中：

```json
{
  "entities": [
    {
      "name": "围观修士",
      "entity_type": "crowd",
      "episode_keys": ["episode_001"],
      "description": "聚在青霄宗山门外观望冲突的低阶修士人群。",
      "visual_notes": ["青灰道袍", "远景", "不可辨认具体面孔"]
    }
  ]
}
```

## 13. 关键结论

- 不再使用 `lead/main/supporting/minor/background` 作为核心流程判断。
- 收敛为 `primary / functional / ambient` 三层。
- 只抽取 `primary` 和 `functional` 进入 `roles`。
- `primary` 与 `functional` 必须分节点抽取。
- 功能角色抽取时，已抽取列表必须分成 `primary_roles` 和 `functional_roles` 两个输入。
- 背景存在不进入角色系统，应作为独立背景实体服务场景和分镜。
- 下游资产策略必须最终基于 `role_tier` 控制成本。
