"""LangGraph-ready workflow modules.

The current MVP uses a direct async runner. The node boundaries are intentionally
kept explicit so this can be moved into LangGraph without changing services.
"""
