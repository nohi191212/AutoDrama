审查输入视频是否可直接用于成片。

镜头：{{shot_name}}
预期：{{expectation}}
当前视频提示词：{{current_prompt}}

检查动作是否自然连贯、节奏是否稳定、角色身份是否漂移、肢体和物体结构是否错误，以及是否出现字幕、logo、水印、假文字或无关主体。

只输出 JSON：
- `approved`：只有视频可直接使用时为 true。
- `issues`：不通过时列出具体可见问题；通过时为空数组。
- `revised_prompt`：不通过时写一条完整、简洁、可直接交给视频模型的中文修订提示词；通过时为空字符串。它只描述最终可见的镜头，不能写审计说明、项目资料、字段或工作流程。
- `rationale`：一句简短判断依据。
