审查输入视频是否可直接用于成片。

镜头：{{shot_name}}
预期：{{expectation}}
当前视频提示词：{{current_prompt}}

同时检查画面和原生音频：

- 有对白时，逐字核对内容、顺序、说话人、首次开口时间和嘴型；说话应与镜头表演同步开始，不得出现独立的无声开场，也不得漏词、重复、串音或让非说话者动嘴。
- 无对白时，人物必须始终闭嘴，不能擅自增加人声。
- 只按“预期”和“当前视频提示词”中已有的情节、角色、动作与运镜审查，不得要求输入中没有的新动作、新表情或新镜头设计。
- 对静默动作镜头，标出既有主动作完成时间、既有后续动作结束时间和纯停留长度；动作不得被拉长填满视频，也不得过早完成后长时间空等，结尾应短暂、稳定且仍有自然生命感。
- 运镜应服务于输入中已有的动作并随其落点结束；物体运动应遵守接触、支撑、重量转移和释放关系，不得出现融合、穿插、悬浮、自发运动或形态漂移。
- 检查角色身份与服饰漂移、肢体结构错误，以及字幕、logo、水印、假文字或无关主体。

只输出 JSON：
- `approved`：只有视频可直接使用时为 true。
- `issues`：不通过时列出具体可见问题；通过时为空数组。
- `revised_prompt`：不通过时写一条完整、简洁、可直接交给视频模型的中文提示词；通过时为空字符串。完整保留当前提示词中已有的情节、角色、动作和镜头意图，不新增任何姿态、视线、表情、动作、道具行为或镜头设计；只用更直接的生成表达调整时序、运动幅度和物理质感。对白及声源信息放在最前，已有表演与对白同步，已有运镜低幅度跟随，物理关系清楚，结尾短暂稳定。只描述最终可见可听的镜头，不写审计过程、项目资料、字段、规则解释或工作流程。
- `rationale`：一句简短判断依据。
