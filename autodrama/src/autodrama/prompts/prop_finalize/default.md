审计并规范下列完整道具表：

{{props}}

返回完整、去重后的 `props` 及简短 `merge_notes`，严格符合 JSON Schema。

合并同一物件的别称；移除人物、服装、场景结构和纯氛围项。每组必须有 `base`；variant 只表示同一物件有明确可见变化，引用同组 base，`state_change` 只写变化。保留真正独立的道具，不因临时位置或持有人拆分。名称短且稳定，外观客观可画，不写生图提示或技术字段，不自造 episode_key。
