审查输入图片 1 是否可直接作为{{asset_type}}资产进入视频生成链路。

资产：{{asset_name}}
预期：{{expectation}}
当前图像提示词：{{current_prompt}}

检查画面是否准确表达预期、风格是否统一、主体或空间是否完整可用，以及是否出现瑕疵、畸形、额外主体、文字、水印、logo、拼图、多视图、写实摄影或 3D 产品渲染等问题。

只输出 JSON：
- `approved`：只有画面可直接使用时为 true。
- `issues`：不通过时列出具体可见问题；通过时为空数组。
- `revised_prompt`：不通过时写一条完整、简洁、可直接交给图像模型的中文修订提示词；通过时为空字符串。它只描述最终可见画面，不能写审计说明、项目资料、字段或工作流程。
- `rationale`：一句简短判断依据。
