审查角色候选与剧本文本是否匹配；只审查，不新增或改写角色。

剧本文本：
{{novel_full_context}}

主要角色候选：
{{primary_roles}}

功能角色候选：
{{functional_roles}}

仅报告三类确定问题：候选实际出现却漏掉的 `episode_keys`（只追加）、同一角色的重复候选（保留名称必须来自候选）、以及不应生成视觉资产的功能角色（背景群体、纯画外音、未具象化人物或与主要角色重复项）。检查每个保留角色是否有合理的 `base` 造型，状态变化是否正确指向同一角色的 `variant`；无法确定时不要报告。`episode_keys` 只能使用文本中的实际 key。仅输出符合给定 JSON Schema 的 JSON；没有问题时使用空数组。
