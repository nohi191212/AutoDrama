核对候选角色与剧本文本是否一致。只修正候选整理问题，不新增人物，也不改写角色设定。

剧本文本：
{{novel_full_context}}

候选主要角色：
{{primary_roles}}

候选功能角色：
{{functional_roles}}

只报告能够由正文确认的三类问题：角色实际出现但候选遗漏了出场范围；同一角色被重复列出；背景群体、纯画外音、未具象化人物或与主要角色重复的功能角色被误建为视觉资产。保留名称必须来自候选，出场范围只能追加正文已有的片段标识。每个保留角色应有合理的基础造型，持续状态变化应正确引用同一角色的基础身份。无法确定时不要报告。只输出符合给定 JSON Schema 的 JSON；没有问题时返回空结果。
