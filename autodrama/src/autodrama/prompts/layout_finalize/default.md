审计并规范下列完整场景表：

{{layouts}}

返回完整、去重后的 `layouts` 和简短 `merge_notes`，严格符合 JSON Schema。

合并同一物理空间的别称、局部和镜头角度；删除非空间项。每个空间必须有一个 `base`，稳定拓扑和固定环境锚点写在其 `space_features`。有明确门/墙边界或独立表演功能的院落与室内、教室与庭院、内室与灶房应保留为不同 base，即使同属一个宅院。variant 只表示不改变空间拓扑的明确可见状态，引用对应 base，`state_delta` 只写变化。不要因人物站位、临时道具或镜头变化建变体；不自造场景或 episode_key，不写提示词或技术字段。
