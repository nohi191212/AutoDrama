# 任务

把下面的完整视频镜头描述提炼为一个稳定、可生成的开场关键帧描述。

完整镜头描述：
{{video_prompt}}

内部站位计划：
{{blocking_plan}}

全局视觉质量：
{{visual_quality}}

只返回包含单一字段 `prompt_content` 的 JSON。`prompt_content` 使用中文，只描述镜头开始时已经成立的画面状态：主体姿态、表情、视线、接触、遮挡、景深和光影融合。把连续动作收敛到动作尚未展开或刚要发生的开场瞬间；忽略对白、旁白、声音和未来动作。不要重新设计机位或背景，不要叙述完整动作过程。

不要在 `prompt_content` 中写角色名、binding_id、ref_id、Image 编号或参考图职责；这些绑定由工作流代码确定性追加，禁止模型重新解释。
