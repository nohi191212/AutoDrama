阅读下列多个独立章节 TXT，为后续制作整理剧情概要、角色、关键物件和主要空间。只依据正文原文，不改写、扩写、推断或补充未出现的内容。

输入格式：每个章节先显示其源文件名 `chap####_章节名.txt`，下一行开始是该章节正文。章节顺序与标题以文件名为准，正文不包含章节标题。

原始章节文本：
{{raw_script}}

返回由系统提供结构约束的纯 JSON，不要使用 Markdown 或额外说明。内容要求：

- 给出整体剧情和每个输入章节的概要，`episode_outlines` 必须严格按输入文件顺序返回，数量与输入章节数量完全一致。每个章节只对应一个章节概要。
- 只保留实际出现、说话、行动或直接推动剧情的具体命名角色；无名群众可作为群体提及，但不得虚构具名角色。稳定身份与不同时间阶段的年龄、外观必须分开描述；动作、情绪、伤势、持有物和后续事件不得写成永久身份。
- 提取实际可见、拿取、展示、交接、归还、使用或激活并推动剧情的关键物件。只写物件的稳定身份特征，不把某一时刻的持有人或状态写成永久属性。
- 只提取实际发生关键事件的物理空间。空间描述只写可见的结构、功能和环境，不写镜头语言、人物动作、固定画风、模型信息或未来事件。
- `notes` 只记录确实会影响后续制作的原文歧义；没有则返回 `null`。
