从下列原始剧本文本中提取结构化剧情信息。只依据原文，不改写、扩写或补充未出现的内容。

原始剧本文本：
{{raw_script}}

输出符合给定 JSON Schema 的纯 JSON。内容要求：

- `outline` 为 300–800 字的整体剧情概要，包含人物、冲突、转折、目标和结尾状态；不要写成宣传语、分镜或旁白。
- `episode_outlines` 按原文实际剧集或章节划分；单集成熟剧本只输出一个条目。每项概括主要事件、人物行动、冲突推进和结尾悬念或状态。
- `roles` 只保留在当前剧情中实际出现、说话、行动或直接推动剧情的具体命名角色/生物。不要把群体、纯背景提及或未登场设定写成角色。外观线索、别称和证据必须来自原文；`primary` 仅用于主角、核心亲属、师长或持续影响主线者。
- `props` 只保留剧情中实际可见且需要拿取、展示、激活或推动情节的物件；忽略普通陈设、衣物和仅被台词提到的物件。`status` 只写原文明示的关键状态，否则为 `normal`。
- `layouts` 只保留剧情实际发生且有角色行动或关键事件的空间；同一地点的不同镜头不要拆分。`prompt` 只写简短、可见的空间特征，不写镜头参数、画幅、模型名或风格词。
- 所有 `evidence` 简短说明原文依据；没有可靠信息时用空数组或空字符串。不要输出完整剧本文本、Markdown 或额外字段。
