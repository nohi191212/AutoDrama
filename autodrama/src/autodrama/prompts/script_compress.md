# 任务

将最终剧本压缩为后续场景、分镜和视频生成可复用的简约剧本。

# 输入

标题：{{title}}

最终剧本：
{{final_script}}

必须使用的分集键名：{{episode_keys}}

# 要求

- `simple_script` 必须包含且只包含这些键名：{{episode_keys}}。
- 每集简约剧本保留场景变化、角色行动、关键道具、冲突和结尾钩子。
- `global_script` 写全局故事摘要，方便跨集保持人物、场景和道具一致。
- 不要加入旁白。
- 输出必须符合调用方提供的 JSON schema。
