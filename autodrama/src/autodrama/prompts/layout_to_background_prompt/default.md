依据图1的场景母版，为下列镜头规划最少但充分的单画面无人背景图。图1只用于保持空间结构、入口门窗、固定家具、材质、比例、时间与光线的一致性。

场景描述：
{{layout_description}}

待规划镜头：
{{shots}}

只输出 JSON。顶层 key 为连续的 `background_1`、`background_2`……。每项只能包含：
`prompt_content`、`shot_indices`、`description`。

规则：
- 每个输入镜头索引必须且只能出现在一个 `shot_indices` 中。
- 仅在同一空间状态、机位区域、朝向、叙事角度、景别和透视都兼容时复用背景。正反打、主客观视角切换、跨轴、明显俯仰变化必须分开。
- `description` 是供人审核的一句话叙事描述，可提及人物关系和观察方向。
- `prompt_content` 直接描述一张无人电影背景：场景、机位、朝向、景别、空间层次、光线和固定陈设。不得出现人物、人体局部、文字、字幕、logo、水印、三视图、拼图、宫格、UI 或参考表。
