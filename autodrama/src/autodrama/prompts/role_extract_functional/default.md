从剧本文本中补充尚未列出的功能角色。

剧本文本：
{{novel_full_context}}

主要角色：
{{primary_roles}}

已抽取功能角色：
{{functional_roles}}

只输出非主要角色、但在当前剧情中有独立动作、对白、直接互动或需要跨镜头识别的具体角色或生物。不得重复主要角色、别称、背景群体、纯氛围存在或仅被提及的设定。画外音、旁白、屏外声音、抽象意识、未露出可识别身体的记忆人物，不是角色视觉资产，绝不输出。无名角色仅在确有独立功能时使用具体的“限定词+身份”名称。所有输出的 `role_tier` 为 `functional`，`episode_keys` 只能使用正文中的实际 key。

每个角色提供一个 `base` 造型；仅在原文明示且需复用的同一角色状态变化时增加 `variant`。外观信息必须客观、可见且有原文依据。没有新增角色则输出空 `roles`。仅输出符合给定 JSON Schema 的 JSON。
