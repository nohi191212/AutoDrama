审查输入图片 1 是否可以作为生产链路中的主视觉锚点。

资产：{{asset_name}}
预期与视觉合同：{{expectation}}
镜头合同：{{shot_contract}}
场景专属风格合同：{{scene_style_contract}}
实际用于生成图片的提示词：{{current_prompt}}

以下是本次必须使用的生产审计 rubric：

{{rubric}}

审计要求：

1. 对 rubric 中每个维度恰好输出一条 assessment；确实不适用于当前画面的维度使用 `applicable=false` 和 `score=null`，不能因为证据被裁切、遮挡、虚焦或无法确认而标为不适用。
2. 适用维度按 0–10 分评价。每项都写可观察证据；任何低于 10 分的项目都写具体缺陷。`major` 或 `critical` 缺陷必须给出至少一个归一化区域 `x1,y1,x2,y2`。
3. 只按加权公式形成总分。不得因为 gate、严重度、单项低分或所谓 hard failure 对总分进行 4.9、5.9、6.9 或任何其他封顶、截断、替换。
4. `approved=true` 仅当未封顶加权总分不低于 {{approval_threshold}}、不存在 `critical` 缺陷，且所有标为 `gate=true` 的适用维度都达到生产协议中的 gate 线。gate 与严重度可以阻止交付，但绝不能修改总分。
5. 必须核对画面与镜头合同、场景专属风格合同及实际提示词中的身份、人数、动作、左右、持有、空间、相机和风格要求；不得把三者未要求的事实自行当成合同。
6. `issues` 按严重度列出可见问题。若不通过，`revised_prompt` 必须是一条完整、可直接交给图像模型的修订提示词，继续服从两份合同，保留原提示词中已经成立的全部事实，只针对可见缺陷做最小修复；若通过则为空字符串。

只输出符合 schema 的 JSON，不要输出 Markdown、解释文字或另一套分数。`weighted_score` 与 `category_scores` 可按你的计算填写，但生产代码会从逐维分数重新计算，最终以代码计算值为准。
