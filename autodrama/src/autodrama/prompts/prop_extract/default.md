根据剧本维护完整、可复用的剧情道具表。

剧本：
{{novel_full_all_episodes}}

已有道具：
{{existing_props}}

只保留会被拿取、展示、激活、损坏或作为关键视觉线索的独立物件；排除角色、生物、普通衣物、背景陈设和固定建筑/地形。合并同一物件的别称，只有可见状态发生关键变化时才建 variant，并让 variant 引用 base。每个返回的 prop 必须提供非空的 `assets` 数组，至少包含一个 `asset_role="base"` 的资产（通常命名为 `base`）；即使道具只有一种状态，也必须建立这个 base。不得省略 assets、返回空数组或只返回道具名。已有合理资产保留。

外观只写剧本有依据的可见结构、材质与状态，不补设定。书页、试卷、印文等文字信息只用不可辨读的笔迹、纹样、符号或光效表达，不把可读文字当作图像资产要求。

返回完整 `props` 及可选简短 `notes`，严格符合给定 JSON Schema；episode_keys 只能使用剧本中的 key。不要写生图提示、镜头参数、模型信息或工作流说明。
