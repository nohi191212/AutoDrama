比较输入图片 1 的角色身份板与输入图片 2 的主视觉人物设计。输入图片 3 是三视图空间模板，只用于识别模板人物污染。

目标不是把角色做成主视觉里的同一个人，而是判断两者是否属于同一项目的人物设计与渲染语言。不要因为复制了主视觉中的具体脸、发型、服装、道具、姿势或场景而提高相似度；出现这种复制时必须判定对应硬门槛失败。

当前角色必须保持的内容：
{{role_contract}}

逐项观察下面的相似度标准。每项使用 0 到 10 分，并写出主体、区域、材质或光影关系以及造成该分数的可见结果。图片中看不清但本应可见的内容按低分处理，不要根据生成提示词推断成功。

{{rubric_text}}

同时判断下面的交付硬门槛是否通过，并为每项写出图片证据。

{{gate_text}}

只输出符合给定 JSON Schema 的 JSON，不输出 Markdown、代码围栏或前后说明。每个相似度维度和硬门槛必须恰好出现一次。总体判断只总结图片证据，不讨论模型、字段、项目路径或工作流。
