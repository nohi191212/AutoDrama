# 任务

在已抽取主要角色之外，循环抽取“功能角色”。本节点只输出 `role_tier=functional` 的角色。

# 输入

标题：{{title}}

原始故事：
{{raw_script}}

完整小说正文（按集组织，每集可能包含“源章节：第X章-第Y章”等信息）：
{{novel_full}}

Clip 片段（第一阶段按建议 8-15 秒画面单位拆分的剧情节奏，用于校验功能角色是否有独立镜头功能；不得替代完整小说正文）：
{{clip_segments}}

项目约束（只用于保持功能角色边界、场面调度和不能变的事实；角色是否出现仍以完整小说正文为准）：
{{project_context}}

主要角色列表（这些人物绝对不要作为功能角色重复输出）：
{{primary_roles}}

已抽取功能角色列表（每项是 `[角色名, 一句话识别说明]`，用于避免重复输出）：
{{functional_roles}}

全集 episode_key 列表：{{episode_keys}}

# 判定顺序

先判断是否主要角色，再判断是否功能角色，最后判断是否背景存在。

如果某个人满足主要角色条件，即使某场戏里只是在传话、递药、阻拦，也不要放入功能角色。主要角色列表中的人物不要换称谓重复输出。

# 功能角色定义

功能角色是不承担长期人物线、通常只服务于一个或少数场景，但有明确独立动作、对白、交互、信息传递、阻碍、引导、战斗、救治等功能的人。

判断核心：

```text
如果删除这个人，会导致某个镜头动作、对白、信息传递或事件触发变得不清楚，那么这个人就是功能角色。
```

满足任一条即可归为功能角色：

- 有独立动作并推动当前场景：阻拦、盘问、开阵、引路、通报、递交、治疗、追杀、押送、交易。
- 有独立台词、喊话、传音、命令、警告。
- 与主要角色发生直接互动。
- 是镜头焦点的一部分，需要观众识别这个人正在做什么。
- 后续分镜需要引用这个具体身份。

玄幻语境示例：

```text
山门守卫
传令剑侍
青衣药童
戒律堂执事
黑甲妖兵甲
妖市摊主
白发丹师
押阵魔修
灵舟舵手
试炼主持弟子
```

功能角色可以有名字，也可以无名。关键不是有没有名字，而是有没有短期场景功能。无名功能角色命名要使用“限定词 + 身份”，例如 `山门守卫`、`戒律堂执事`、`青衣药童`、`黑甲妖兵甲`、`妖市摊主`、`传令剑侍`。

# 不要输出

- 主要角色列表中的任何人物。
- 只换称谓后的主要角色，例如把“沈烬”写成“少年剑修”重复输出。
- 纯背景群体，例如围观修士、众弟子、魔兵们、妖市行人、宴席宾客、长老席众人、低阶妖族。
- 没有单独动作、没有单独台词、没有明确互动、只营造场面气氛的背景存在。

# 输出要求

- 本轮只输出“已抽取功能角色列表”中尚未包含的新增功能角色。
- 如果全本完整小说中已经没有新增功能角色，输出空的 `roles` 数组。
- `role_tier` 固定为 `functional`。
- `name` 必须使用小说原名或稳定身份称谓；不要用“路人”“众人”“某修士”“一个人”这类泛称。
- `episode_keys` 是必填字段，必须是非空数组；数组值只能从“全集 episode_key 列表”逐字复制，例如 `episode_001`。
- 判断 `episode_keys` 时只依据“完整小说正文”这个 JSON 的外层 episode key；功能角色出现在哪个 episode 的正文里，就填写哪个 episode key。
- 如果无法从完整小说正文中确定某个功能角色对应的 episode_key，不要输出这个角色，留到后续轮次或人工处理；不要输出空数组、`null`、中文章节名或自造 key。
- 如果功能角色跨多集出现，按输入 episode_key 顺序列出所有实际出现或被明确提到并影响剧情的集。
- `source_chapters` 填写该角色出现的源章节范围或章节号；无法判断时填空数组。
- `brief` 用一句话说明该人物的短期场景功能和与主要角色的互动。
- `appearance_notes` 只写短期分镜需要稳定识别的基础外观、身份装束、持物或动作特征。
- `has_dialogue` 表示此角色在已给正文中是否有明确对白、喊话、传音或命令。
- `visual_reuse_required` 只在该功能角色需要跨镜头保持稳定外观时为 `true`，否则为 `false`。
- 不要输出 `importance` 字段，不要使用 `lead/main/supporting/minor/background` 五级分类。
- 输出前自检：每个 `roles[]` 条目都必须有非空 `name`、`role_tier="functional"`、非空 `episode_keys`，且 `episode_keys` 全部属于全集 episode_key 列表；不满足就删除该条目。
- 输出必须符合调用方提供的 JSON schema。
