判断输入图片 1 能否直接作为角色身份板使用。

目标角色：
{{asset_name}}

预期画面：
{{expectation}}

生成时使用的提示词：
{{current_prompt}}

参考图片说明：
{{reference_context}}

这是宽松的生产兜底审查，不是审美评审。只根据图片中实际可见的结果判断，并且只在画面明显不可作为角色身份参考时拒绝：缺少正面、真侧面、背面中的任何一个完整全身视图，出现多余视图、额外主体、明显裁切或严重重叠；三个视图明显不是同一个角色；脸部、身体或肢体存在一眼可见的严重畸形；角色的基本年龄阶段、性别呈现或主体类型与预期完全不符；提供图片2时，图片1与图片2明显不可能是同一角色的不同阶段，或者明显照搬了只属于参考阶段的年龄特征；出现明显文字、标签、水印、logo 或严重画面脏污。

普通长相、标准美型、设计不够独特、不同角色之间有些相似、跨年龄骨相只有大致连续性、轻微手脚问题，以及服装、材质、色彩、装饰或视觉风格上的一般偏差，都不是拒绝理由。只要主体没有明显崩坏、身份大体正确、三视图基本可用，就应通过；拿不准时优先通过。

只输出符合给定 JSON Schema 的 JSON。通过时 `approved` 为 `true`，`issues` 为空数组，`revised_prompt` 为空字符串，并在 `rationale` 中用一句简短中文说明人物没有明显崩坏且三视图基本可用。不通过时只记录上述明显不可用的问题，并给出一条完整、简洁、可直接发送给图像模型的中文修订提示词。修订提示词只描述需要得到的最终画面，不写审计过程、字段、项目资料或工作流。
