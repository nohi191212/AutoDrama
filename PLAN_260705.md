# Prompt 模块与镜头背景/关键帧链路破坏性重构规划

## 1. 文档目标

本文档规划两项必须一起完成的重构：

1. 重构整个 Prompt 模块，使所有实际发送给模型的提示词都能被定位、复现和审计。
2. 重构 shot 背景图与关键帧链路，使每个镜头拥有且仅拥有一个叙事角度，并允许多个镜头复用同一张镜头背景图。

本文档只描述目标架构、数据合同、实施顺序和验收标准，不包含代码实现。

## 2. 破坏性重构声明

本次允许并计划进行破坏性重构，不要求兼容旧节点接口、旧模板路径、旧 schema 或旧生成产物。

具体原则如下：

- 不为旧的扁平 Prompt 路径保留兼容读取。
- 不为旧节点名、旧字段名或旧 JSON 结构增加 alias。
- 不编写旧产物迁移脚本。
- 不读取或合并旧的 `shot_keyframe_prompt`、`shot_keyframe_image_generation` 等节点输出。
- 不复用旧的“一镜头一背景”产物。
- 不保留 `shot_keyframe_image_generation` 内部先生成背景、再生成关键帧的旧职责。
- 重构完成后，相关项目必须从受影响的最早节点重新生成。
- 旧产物若残留，运行时应因 schema 或依赖不匹配而明确失败，不静默降级。
- 文档、示例配置、默认节点链路和 smoke 脚本只描述新架构，不继续说明旧用法。

“破坏性重构”只针对本规划覆盖的数据合同、节点、Prompt 模块和生成产物。实施时仍需保留工作区中与本任务无关的用户修改。

## 3. 核心设计原则

### 3.1 裸模直用

所有 Prompt 都直接发送给模型 API，不依赖 Agent 解释上下文。因此 Prompt 必须只包含模型完成当前内容任务所必需的信息。

不得因为担心模型输出技术字段，就先把大量技术字段交给模型，再用“不要引用这些字段”进行补救。正确方式是从输入中移除无关字段。

### 3.2 内容和运行时元数据分离

以下内容默认不得进入 Prompt：

- 项目 ID、项目目录、文件路径、URL。
- 节点名、上游节点名、下游节点名。
- “代码会处理”“后续节点会重试”等工作流说明。
- Provider 名称和模型名称，除非该模型确实需要专属语法。
- 日志字段、重试轮次、内部异常栈和内部资产 ID。
- 当前是第几集、项目总集数等与当前内容任务无关的信息。

以下内容在直接影响输出时允许保留：

- 模型必须返回的 JSON 字段名。
- 本次调用内的局部镜头索引。
- 用于绑定参考图的图像顺序或占位符。
- 实际影响节奏的目标时长。
- 实际影响画面的角色、道具、场景、构图和风格约束。

### 3.3 每个镜头只有一个叙事角度

每个 shot 必须拥有一个非空的 `narrative_angle`。

叙事角度回答：

- 观众从谁或什么的方向观察？
- 画面主要让观众关注谁或什么？
- 摄影机朝向哪一侧？

一个 shot 不得同时声明多个互相竞争的叙事角度。如果剧情需要切换叙事角度，应拆成不同 shot。

### 3.4 背景描述和背景生图 Prompt 分离

镜头背景记录同时保留：

- `description`：供人审计的叙事性一句话描述，可以提到角色关系，例如“阿林和思杭在卧室中对话，镜头从思杭一侧朝向阿林”。
- `prompt`：直接用于生成无人背景图，只描述场景、机位、镜头朝向、景别、空间层次、光线和固定陈设，不生成角色。

不得把“描述可以提到角色”误解为“背景图中应出现角色”。

### 3.5 最终 Prompt 可审计

日志保存的必须是所有模板渲染、模型输出校验、通用前后缀拼接和安全改写完成后，实际作为 `prompt` 参数发送给 API 的完整字符串。

日志中的 Prompt 与 Provider 收到的 Prompt 必须逐字符一致。

## 4. 术语

| 名称 | 含义 |
| --- | --- |
| Prompt 模板 | 节点目录中的 `.md`，用于向模型说明当前内容任务 |
| Prompt renderer | 节点目录中的可选 `render.py`，对模型输出做确定性前后缀组装 |
| 最终 Prompt | 实际作为 `prompt` 参数发送给某次模型 API 的完整文本 |
| 场景母版图 | `layout_image_generation` 生成的同一空间多视角全景/三视图参考图 |
| shot | 一次 3–15 秒的视频生成任务 |
| 叙事角度 | 当前 shot 唯一的观察方向和叙事关注点 |
| 镜头背景图 | 从场景母版图投射出的单画面无人背景，可被多个兼容 shot 复用 |
| shot 关键帧 | 镜头背景图与角色图、必要道具图融合得到的 shot 起始帧 |
| 资产类型 | Prompt 审计目录的一级分类，如 `layout`、`shot_background`、`shot_keyframe` |
| 资产名 | 当前资产在系统中的稳定唯一名称，用作 `.prompt.txt` 文件名 |

## 5. Prompt 模块目标结构

### 5.1 按节点分目录

现有 `prompts/` 下的扁平模板全部迁移为节点目录：

```text
autodrama/src/autodrama/prompts/
├── script_import/
│   └── default.md
├── clip_to_shots/
│   └── default.md
├── layout_to_background_prompt/
│   ├── default.md
│   └── render.py
├── shot_keyframe_prompt/
│   ├── default.md
│   └── render.py
├── roleboard_prompt/
│   ├── default.md
│   ├── toapi_gpt_image_2.md
│   ├── rightcode_gpt_image_2.md
│   ├── volcengine_seedream.md
│   └── render.py
├── clip_video_generation/
│   ├── default.md
│   ├── kling.md
│   ├── volcengine.md
│   └── render.py
└── ...
```

规则：

- 每个使用 Prompt 的节点拥有独立目录。
- 单模板统一命名为 `default.md`。
- Provider 或模型确实需要不同提示语法时，使用同目录下的额外 `.md` 变体。
- 不再支持根目录下的 `<node>.md`。
- 不再通过多个分散目录表达同一节点的模板变体。
- 节点代码中不再拼接大段 Prompt。

### 5.2 `.md` 的职责

`.md` 只负责告诉模型当前要完成的内容任务，包括：

- 模型角色和专业能力。
- 当前必要输入。
- 内容分析或生成要求。
- 必须遵守的输出结构。
- 直接影响结果的质量约束。

`.md` 不负责：

- 解释工作流或节点关系。
- 解释代码稍后如何处理输出。
- 记录项目、路径或重试元数据。
- 给模型展示其不需要知道的内部字段。
- 拼接最终图片或视频 Prompt 的固定前后缀。

### 5.3 `render.py` 的职责

`render.py` 为可选文件，一般用于“模型先生成内容，再形成最终资产生成 Prompt”的节点。

标准概念接口：

```text
render(model_output, context) -> final_prompt
```

`render.py` 只能执行确定性操作：

- 添加固定的通用前缀或后缀。
- 合并经过允许的全局视觉质量要求。
- 合并通用负向规则。
- 按参考图顺序生成“图1、图2……”的语义说明。
- 删除空段落和完全重复的片段。
- 对空白、换行做稳定规范化。

`render.py` 不得：

- 调用任何模型。
- 根据剧情重新推理或改写内容。
- 修改模型输出的叙事意义。
- 读取文件以补充未显式传入的业务上下文。
- 加入项目 ID、episode key、节点名、路径等无关信息。
- 承担资产查找、状态读写或 schema 校验。

### 5.4 Prompt 生成标准流程

对“文本模型生成资产 Prompt”的节点，统一流程为：

```text
读取 prompts/<node>/<variant>.md
→ 渲染内容任务 Prompt
→ 记录本次文本模型最终 Prompt
→ 调用文本/多模态模型
→ 校验结构化输出
→ 调用可选 render.py
→ 得到下游图片/视频/音乐模型的最终 Prompt
→ 记录该资产的最终 Prompt
→ 调用资产生成 API
```

对“文本模型直接生成结构化业务内容”的节点，流程为：

```text
读取 prompts/<node>/<variant>.md
→ 渲染最终 Prompt
→ 记录最终 Prompt
→ 调用模型
→ 校验结构化输出
```

### 5.5 Prompt 解析器重构

PromptStore 需要改为目录式解析，至少支持：

- 按节点名定位目录。
- 默认读取 `default.md`。
- 按配置选择模板变体。
- 严格检查模板变量是否全部解析。
- 拒绝未知模板变量。
- 可发现并调用可选 `render.py`。
- 对 renderer 使用统一函数签名和返回值检查。
- renderer 返回空字符串时立即失败。

不保留旧扁平路径 fallback。

## 6. 最终 Prompt 日志

### 6.1 目录

每个项目的输出日志下新增：

```text
logs/
└── prompts/
    ├── script_import/
    ├── role_extract/
    ├── roleboard_prompt/
    ├── roleboard/
    ├── prop_prompt/
    ├── prop/
    ├── layout_prompt/
    ├── layout/
    ├── clip_to_shots/
    ├── layout_to_background_prompt/
    ├── shot_background/
    ├── shot_keyframe_prompt/
    ├── shot_keyframe/
    ├── shot_video/
    ├── bgm_prompt/
    ├── bgm/
    └── prompt_safety_rewrite/
```

路径合同：

```text
logs/prompts/<资产类型>/<资产名>.prompt.txt
```

示例：

```text
logs/prompts/roleboard/阿林_base.prompt.txt
logs/prompts/layout/阿林卧室.prompt.txt
logs/prompts/layout_to_background_prompt/阿林卧室.prompt.txt
logs/prompts/shot_background/阿林卧室_朝向阿林.prompt.txt
logs/prompts/shot_keyframe_prompt/episode_001_clip_001_shot_003.prompt.txt
logs/prompts/shot_keyframe/episode_001_clip_001_shot_003.prompt.txt
logs/prompts/shot_video/episode_001_clip_001_shot_003.prompt.txt
```

### 6.2 记录范围

所有调用大模型的资产或逻辑内容都必须记录最终 Prompt，包括：

- 文本结构化生成。
- 多模态文本分析。
- 图片生成。
- 视频生成。
- 音乐生成。
- Prompt 安全改写。
- 其他以 Prompt 为主要输入的模型调用。

Prompt 生成节点和下游资产生成节点分别记录：

- `layout_to_background_prompt/<场景名>.prompt.txt`：发送给多模态文本模型的内容任务 Prompt。
- `shot_background/<背景资产名>.prompt.txt`：发送给图片模型的最终背景 Prompt。
- `shot_keyframe_prompt/<shot_id>.prompt.txt`：发送给多模态文本模型的关键帧 Prompt 生成请求。
- `shot_keyframe/<shot_id>.prompt.txt`：发送给图片模型的最终关键帧 Prompt。

### 6.3 写入时机

Prompt 必须在 Provider 调用边界、调用底层 API 前写入：

```text
final_prompt 已完全确定
→ 原样写入 .prompt.txt
→ 将同一个 final_prompt 对象传给 Provider
```

不得在日志写入之后继续给 Prompt 添加内容。

Prompt 记录器依赖最小运行时元数据：

- `asset_type`
- `asset_name`
- `node_name`，只用于日志归类，不进入 Prompt
- `attempt`，仅重试时使用

### 6.4 重试和安全改写

主文件始终代表最后一次实际发送或最终成功使用的 Prompt：

```text
logs/prompts/<资产类型>/<资产名>.prompt.txt
```

发生重试或安全改写时，额外保留每次实际调用：

```text
logs/prompts/<资产类型>/history/
├── <资产名>.attempt-01.prompt.txt
├── <资产名>.attempt-02.prompt.txt
└── <资产名>.attempt-03.prompt.txt
```

要求：

- 每次真正发出 API 请求都必须有对应历史文件。
- 主文件在每次调用前原子更新，最终保留最后一次版本。
- 安全改写模型自身收到的 Prompt 记录在 `prompt_safety_rewrite/`。
- 安全改写后的资产 Prompt 仍记录在原资产类型目录。

### 6.5 文件格式

- UTF-8 无 BOM。
- LF 换行。
- 原样保留段落。
- 原子写入。
- 不写 API key、认证头或完整请求 payload。
- 不把签名 URL、base64 图片等媒体内容写入 Prompt 文件。
- 参考图只在 Prompt 中用图像顺序和内容语义表示。

### 6.6 文件名

资产名必须使用系统中的稳定唯一业务名称：

- 角色板：`角色名_造型名`
- 道具：`道具名_状态名`
- 场景：`场景名`
- 镜头背景：代码生成的稳定背景资产名
- shot 关键帧和视频：正式 `shot_id`

Windows 非法字符 `<>:"/\|?*` 必须做确定性替换。

同一资产类型下不允许静默覆盖不同资产。如果业务名称不唯一，应在资产命名阶段生成唯一名称，而不是由日志模块临时追加随机后缀。

## 7. 全库 Prompt 审计与治理

### 7.1 审计范围

审计范围包括：

- `autodrama/src/autodrama/prompts/` 下全部 Markdown 模板。
- service 和 workflow node 中动态拼接的文本 Prompt。
- 图片、视频、音乐调用前动态添加的前后缀。
- Prompt 安全改写。
- postgen 素材审计、剪辑计划和成片审计 Prompt。
- Provider 适配层是否在日志记录后再次改变 Prompt。

### 7.2 审计表字段

为每个真实模型调用记录：

| 字段 | 内容 |
| --- | --- |
| node | 调用节点 |
| prompt source | `.md`、`render.py` 或内联来源 |
| model capability | text/image/video/music/judge |
| variables | 模板变量 |
| content-required variables | 直接影响内容的变量 |
| metadata-only variables | 项目、路径、节点、Provider 等无关变量 |
| final asset type | Prompt 日志分类 |
| final asset name | Prompt 日志文件名 |
| action | 保留、重写、拆分或删除 |

### 7.3 已确认需要优先处理的问题

1. `clip_segment` 中的“这个节点”“后续节点”、项目标题和 episode key。
2. roleboard 模板中的 provider、model、项目约束、路径/ID 禁止说明。
3. `clip_storyboard_prompt` 中“代码负责”“本节点不负责”“缺失会重试”等工作流说明。
4. image safety rewrite 中的节点名、资产 ID、内部上下文、Provider 错误和改写轮次。
5. postgen edit plan 中的 `source_path`、`output_path` 和程序 schema 元数据。
6. `shot_keyframe_prompt` 当前没有模型调用，只做本地文本拼接。
7. `shot_keyframe_image_generation` 当前内部临时拼接背景 Prompt。
8. clip video 模板中的节点、后期和 Provider 执行解释，需要改为直接的视频生成要求。
9. storyboard keyframe 模板中的项目措辞和非必要技术标签。
10. BGM、key vision、角色提取等模板中是否重复传入标题、完整项目上下文或 episode 元数据。

### 7.4 Prompt 重写规则

- 以“你需要生成什么”开头，而不是“本节点负责什么”。
- 输入区只出现模型需要理解的内容。
- 不向模型展示调用方随后会做的事情。
- 不告诉模型忽略一个本不该传入的字段。
- 不传项目标题，除非标题本身直接影响创作内容。
- 不传 episode key；模型需要顺序时改用本次调用内的局部索引。
- 不传路径或 URL；媒体通过 refs 传递。
- 不传 Provider/model 名；只有专属模板可内化必要的模型语法差异。
- JSON schema 中只描述调用方确实解析的字段。
- 输出 Prompt 本身不携带内部 ID；代码在模型返回后建立 ID 映射。

### 7.5 扁平模板和内联 Prompt 删除

目录式模板迁移完成后：

- 删除旧的根级 `.md`。
- 删除旧的模板候选 fallback。
- 删除 node/service 中可由 `.md` 表达的大段字符串拼接。
- 删除 Provider 层对 Prompt 的隐式业务前后缀。
- Provider 层只负责协议封装，不负责增加业务语义。

## 8. 新镜头链路

### 8.1 目标默认链路

```text
clip_segment
→ clip_to_shots
→ layout_to_background_prompt
→ shot_background_image_generation
→ shot_keyframe_prompt
→ shot_keyframe_image_generation
→ shot_manifest_generation
→ shot_video_generation
```

新节点采用正确拼写：

- `layout_to_background_prompt`
- `shot_background_image_generation`

不创建 `backround` 拼写的兼容别名。

### 8.2 场景母版合同

`layout_image_generation` 继续生成同一场景的多视角全景/三视图母版，用于理解完整空间。

母版应提供：

- 同一空间的稳定结构。
- 入口、门窗、墙面、地面、主要固定家具的关系。
- 可走动区域。
- 可拍摄方向。
- 一致的材质、时间和光线。
- 足以投射多个单镜头背景的互补视角。

镜头背景不是母版裁切图，而是参考母版空间后生成的单张电影画面。

## 9. `clip_to_shots` 重构

### 9.1 职责

`clip_to_shots` 只负责把连续剧情片段拆成 3–15 秒 shot，并为每个 shot 产生内容和资产引用计划。

它不再负责生成最终关键帧 Prompt。

### 9.2 新模型输出

每个 shot 至少包含：

```json
{
  "shot_description": "阿林和思杭在卧室中继续对话，阿林回答问题",
  "narrative_angle": "以思杭的观察方向看向阿林",
  "opening_state": "阿林站在床尾，思杭位于近景画外侧",
  "ref_ids": ["layout_x", "role_a", "role_b"],
  "video_prompt": "阿林停顿后回答，思杭保持注视",
  "duration_seconds": 7,
  "dialogue": ["阿林：我已经决定了。"]
}
```

删除旧的 `keyframe_prompt` 字段，避免 shot 规划阶段提前承担最终画面合成。

### 9.3 叙事角度要求

- `narrative_angle` 必填。
- 一个 shot 只能表达一个叙事关注方向。
- 对话正反打应拆成不同 shot。
- 主观视角、越肩、反应镜头、环境建立镜头和细节插入镜头都要明确唯一关注点。
- 如果一个描述同时要求“看向阿林”和“切到思杭反应”，必须拆分。

### 9.4 模型局部索引

模型只接收当前 clip 内的局部连续索引，不接收 episode key 或正式 shot ID。

代码在校验后生成正式：

```text
episode_001_clip_001_shot_001
```

局部索引和正式 ID 的映射不进入 Prompt。

## 10. `layout_to_background_prompt`

### 10.1 职责

根据一个场景母版图和属于该场景的 N 个 shot，规划需要生成的最少但充分的镜头背景图。

一个背景可以服务多个 shot，但每个 shot 必须且只能映射到一个背景。

### 10.2 调用粒度

按 layout 分批调用多模态文本模型：

- 一张场景母版图。
- 该 layout 下的 shot 列表。
- 每个 shot 的局部索引。
- `shot_description`。
- `narrative_angle`。
- `opening_state`。
- 只在影响背景规划时提供关键固定道具说明。

不传：

- 项目名。
- episode key。
- 正式 shot ID。
- 资产路径。
- Provider/model。
- 下游节点说明。

### 10.3 模型输出

模型输出一个以局部背景 ID 为 key 的字典：

```json
{
  "background_1": {
    "prompt_content": "卧室内部的单张无人电影背景，机位位于思杭一侧，平视中景，朝向床尾……",
    "shot_indices": [1, 3, 4],
    "description": "阿林和思杭在卧室中对话，镜头从思杭一侧朝向阿林"
  },
  "background_2": {
    "prompt_content": "卧室门口方向的单张无人背景，略低机位，朝向走廊……",
    "shot_indices": [2],
    "description": "人物准备离开卧室，镜头朝向房门与走廊"
  }
}
```

模型只返回内容和局部映射。代码负责：

- 生成正式 background ID。
- 将局部 shot index 映射到正式 shot ID。
- 调用 `render.py` 生成最终背景图片 Prompt。
- 保存节点输出。

### 10.4 背景复用条件

只有以下条件兼容时才允许复用：

- 同一 layout 和同一时间/光线状态。
- 摄影机位于相同区域。
- 摄影机朝向相同。
- 叙事角度相同或完全兼容。
- 景别和焦距差异不会改变主要透视关系。
- 前中后景固定结构一致。
- 不需要为某个 shot 改变固定陈设或主要遮挡。

以下情况必须拆成不同背景：

- 对话正打和反打。
- 主观视角与客观视角。
- 机位跨越人物轴线。
- 平视与明显俯拍/仰拍。
- 房间内部朝门与门口朝房间。
- 同一位置但光线状态发生剧情性变化。
- 特写所需的背景透视与全景不兼容。

### 10.5 校验

代码必须校验：

- 输出至少一个背景。
- 每个输入 shot index 恰好出现一次。
- 不存在重复 shot index。
- 不存在未知 shot index。
- 每个背景至少映射一个 shot。
- `prompt_content` 和 `description` 非空。
- `prompt_content` 不要求生成人物。
- 一个背景只能属于一个 layout。

## 11. `layout_to_background_prompt/render.py`

renderer 输入经过 schema 校验的 `prompt_content`，输出直接发送给图片模型的最终背景 Prompt。

统一补充的内容包括：

- 严格参考场景母版的空间结构、材质、比例和光线。
- 只生成一张单画面电影背景。
- 不生成三视图、拼图、宫格、参考表或 UI。
- 不生成人物、人体局部、文字、字幕、logo 和水印。
- 保留固定建筑与固定陈设。
- 是否保留车辆或可移动物体必须由 shot 内容决定，不使用全局“一律禁止车辆”的粗暴规则。

renderer 不得改变：

- 机位。
- 镜头朝向。
- 叙事角度。
- 景别。
- 空间构图。
- 光线状态。

## 12. `shot_background_image_generation`

### 12.1 职责

该节点只负责生成背景图片，不负责背景规划或 Prompt 内容推理。

输入：

- `layout_to_background_prompt` 的最终背景 Prompt。
- 对应场景母版图。

输出：

```json
{
  "background_id": "layout_x_background_001",
  "layout_id": "layout_x",
  "shot_ids": [
    "episode_001_clip_001_shot_001",
    "episode_001_clip_001_shot_003"
  ],
  "description": "阿林和思杭在卧室中对话，镜头从思杭一侧朝向阿林",
  "prompt": "实际发送给图片 API 的最终 Prompt",
  "asset_path": "assets/images/shot_backgrounds/layout_x_background_001.png"
}
```

### 12.2 复用

- 每个 background ID 只调用一次图片 API。
- 所有映射 shot 引用同一资产路径。
- 局部重跑单个 shot 时，如果其 background 已存在且指纹未变化，不重新生成背景。
- 强制重跑某个 background 时，所有引用它的关键帧都必须失效并重新生成。

### 12.3 输出路径

```text
assets/images/shot_backgrounds/<background_id>.png
assets/json/nodes/shot_background_image_generation/<episode_or_batch>.json
```

节点输出按新 schema 写入，不读取旧的 per-shot 背景记录。

## 13. `shot_keyframe_prompt`

### 13.1 职责

该节点改为真正的多模态文本模型节点。

它根据已经生成的镜头背景图、角色图、必要道具图和 shot 内容，生成最终关键帧画面内容。

### 13.2 输入

每个 shot 单独调用，参考图顺序固定：

1. 图1：当前 shot 对应的镜头背景图。
2. 图2-N：当前 shot 实际出现的角色造型图。
3. 后续图片：必要关键道具图。

文本输入只包含：

- `shot_description`
- `narrative_angle`
- `opening_state`
- 必要的角色名与参考图顺序
- 必要的道具名与参考图顺序
- 直接影响画面的视觉质量要求

不传项目名、episode key、路径、节点名或 Provider/model。

### 13.3 模型任务

模型需要：

- 保持图1的机位、透视、空间结构和光线。
- 把角色放入背景，而不是重新设计背景。
- 根据唯一叙事角度确定主体和视线关系。
- 明确人物在前景、中景和背景的位置。
- 明确朝向、视线、遮挡、肢体状态和动作开始前状态。
- 保持角色图中的身份、脸、发型、服装和体型。
- 保持道具图中的造型和尺度。
- 只描述一张起始关键帧，不描述完整动作过程或视频运镜。

### 13.4 模型输出

模型输出最小结构：

```json
{
  "prompt_content": "保持图1卧室背景和机位不变，将图2阿林放在床尾中景……"
}
```

不让模型输出：

- shot ID。
- background ID。
- asset path。
- Provider/model。
- 项目或 episode 信息。

这些由代码关联。

## 14. `shot_keyframe_prompt/render.py`

renderer 负责将 `prompt_content` 组装成图片 API 最终 Prompt：

- 声明图1是必须保持的背景和机位锚点。
- 声明图2-N的角色/道具身份绑定。
- 合并必要的全局视觉风格。
- 合并人物完整性、身份稳定、无文字水印等通用质量规则。
- 合并配置中的负向规则。

renderer 不得：

- 添加新的剧情动作。
- 改变叙事角度。
- 改变角色数量。
- 改变背景机位。
- 把关键帧写成视频 Prompt。
- 添加工作流或资产路径说明。

## 15. `shot_keyframe_image_generation`

### 15.1 新职责

该节点只负责关键帧图片生成：

- 读取 `shot_keyframe_prompt` 的最终 Prompt。
- 第一张参考图固定为对应 background。
- 后续参考图按 renderer 声明的顺序传入。
- 调用参考图生图模型。
- 保存关键帧。

删除旧职责：

- 不再生成背景。
- 不再临时拼接 background Prompt。
- 不再从 raw keyframe prompt 推断机位。
- 不再把“无前景资产”时的背景文件直接当作关键帧文件。

即使 shot 中无人，也要显式生成或确认一张 shot keyframe 资产，不用同一路径同时承担 background 和 keyframe 两种语义。

### 15.2 输出

```json
{
  "shot_id": "episode_001_clip_001_shot_001",
  "background_id": "layout_x_background_001",
  "ref_ids": ["role_a", "role_b"],
  "prompt": "实际发送给图片 API 的最终 Prompt",
  "asset_path": "assets/images/shot_keyframes/episode_001_clip_001_shot_001.png"
}
```

输出路径：

```text
assets/images/shot_keyframes/<shot_id>.png
assets/json/nodes/shot_keyframe_image_generation/<episode_key>.json
```

## 16. `shot_manifest_generation`

manifest 必须显式记录：

- `shot_id`
- `shot_description`
- `narrative_angle`
- `background_id`
- `background_asset_path`
- `keyframe_asset_path`
- 角色、造型、道具和 layout 引用
- `video_prompt`
- `duration_seconds`
- `dialogue`

不得再从旧 `keyframe_prompt` 字段恢复叙事信息。

每个 shot 必须通过以下完整性检查：

- 恰好一个 narrative angle。
- 恰好一个 background ID。
- background 文件存在。
- keyframe 文件存在。
- 参考资产均可解析。
- video prompt 非空。

## 17. 新 schema 方向

建议拆分为以下独立模型：

```text
ShotPlanItem
LayoutBackgroundPromptModelItem
LayoutBackgroundPromptModelOutput
ShotBackgroundPromptItem
LayoutToBackgroundPromptOutput
ShotBackgroundImageGenerationItem
ShotBackgroundImageGenerationOutput
ShotKeyframePromptModelOutput
ShotKeyframePromptItem
ShotKeyframePromptOutput
ShotKeyframeImageGenerationItem
ShotKeyframeImageGenerationOutput
```

关键原则：

- 模型输出 schema 不含项目级运行时字段。
- 持久化 schema 可以包含正式 ID 和路径。
- 不复用旧 schema。
- 不使用兼容 alias。
- `extra="forbid"`。
- 局部索引到正式 ID 的映射只存在于代码。

## 18. 指纹、缓存和局部重跑

### 18.1 指纹

每个资产调用的指纹至少覆盖：

- 最终 Prompt。
- 参考图资产标识和内容版本。
- Provider/model binding。
- 影响结果的尺寸、比例和质量参数。

### 18.2 依赖失效

```text
clip_to_shots 变化
→ layout_to_background_prompt 失效
→ 受影响 background 失效
→ 引用这些 background 的 keyframe prompt 失效
→ keyframe image 失效
→ shot manifest 失效
```

```text
角色造型图变化
→ 引用该角色的 keyframe prompt 失效
→ 对应 keyframe image 失效
→ background 不失效
```

```text
layout 母版变化
→ 该 layout 的全部 background prompt 和 background image 失效
→ 全部关联 keyframe 失效
```

### 18.3 `--shots`

- `layout_to_background_prompt --shots` 必须收集选中 shot 所需的 layout 上下文。
- 一个背景服务多个 shot 时，选择其中任意一个 shot 都能解析到同一 background。
- 强制重生成该 background 时，所有关联 shot 的下游关键帧都标记失效。
- 未选中且依赖未变化的资产保持不动。

### 18.4 不兼容旧缓存

新指纹和新节点输出不读取旧缓存。第一次运行新链路必须重新生成。

## 19. 配置与模型能力

新增或调整节点配置：

```yaml
nodes:
  clip_to_shots:
    model: <text-model>

  layout_to_background_prompt:
    model: <multimodal-text-model>

  shot_background_image_generation:
    model: <reference-image-model>
    params:
      concurrency: 2

  shot_keyframe_prompt:
    model: <multimodal-text-model>

  shot_keyframe_image_generation:
    model: <reference-image-model>
    params:
      concurrency: 2
```

能力要求：

- `layout_to_background_prompt` 必须支持文本 + 至少 1 张图片输入 + JSON 输出。
- `shot_keyframe_prompt` 必须支持文本 + 多张图片输入 + JSON 输出。
- 两个图片生成节点必须支持参考图。
- model catalog 必须声明真实的图片输入上限。
- 关键帧节点在调用前校验“背景 + 人物 + 道具”总参考图数量。
- 参考图超限时明确失败，不静默丢弃角色或道具。

## 20. 实施阶段

### 阶段 0：冻结新合同

先评审并锁定：

- 节点名称。
- `narrative_angle` 定义。
- shot 新字段。
- 背景复用条件。
- 模型输出和持久化输出 schema。
- Prompt 日志路径。
- renderer 标准接口。
- 参考图顺序。

合同锁定前不开始大规模模板迁移。

### 阶段 1：Prompt 基础设施

1. 重写 PromptStore 为目录式解析。
2. 加入模板变体选择。
3. 加入可选 renderer 加载。
4. 加入 renderer 接口和返回值验证。
5. 加入统一 Prompt 记录器。
6. 在 Provider 调用边界写入最终 Prompt。
7. 支持重试历史 Prompt。
8. 删除旧扁平模板 fallback。

### 阶段 2：全库 Prompt 迁移

1. 建立每个节点的 Prompt 目录。
2. 迁移全部现有 `.md`。
3. 把 node/service 中大段内联 Prompt 移到 `.md` 或 `render.py`。
4. 删除 Agent/工作流措辞。
5. 删除内容无关输入变量。
6. 为 Provider 专属模板保留最少差异。
7. 删除旧模板文件。
8. 对每个渲染后 Prompt 建立快照 smoke。

### 阶段 3：shot 规划合同

1. 修改 `clip_to_shots` 模板。
2. 删除 `keyframe_prompt` 输出。
3. 新增 `shot_description`、`narrative_angle`、`opening_state`。
4. 更新校验和 fake provider 输出。
5. 更新 shot 持久化 schema。

### 阶段 4：背景规划与背景生成

1. 实现 `layout_to_background_prompt` 多模态节点。
2. 实现其 `.md` 和 `render.py`。
3. 实现 shot 全覆盖和唯一映射校验。
4. 实现正式 background ID 映射。
5. 实现 `shot_background_image_generation`。
6. 实现背景复用、指纹和局部重跑。
7. 保存背景最终 Prompt。

### 阶段 5：关键帧 Prompt 与关键帧生成

1. 把 `shot_keyframe_prompt` 改为多模态文本节点。
2. 实现参考图顺序合同。
3. 实现 `.md` 和 `render.py`。
4. 重写 `shot_keyframe_image_generation` 为纯关键帧生图节点。
5. 删除旧的内部背景生成逻辑。
6. 保存关键帧 Prompt 请求和图片最终 Prompt。

### 阶段 6：manifest、CLI、配置和文档

1. 更新默认 PREGEN_NODES 顺序。
2. 更新 `--only`、`--episodes`、`--shots` 支持范围。
3. 更新 config 示例和 model catalog。
4. 更新 README、PREGEN_NODES 和节点依赖文档。
5. 删除旧节点说明、旧字段和旧运行示例。
6. 明确要求相关项目从头生成受影响产物。

### 阶段 7：删除旧链路残留

1. 删除旧扁平 Prompt 文件。
2. 删除旧 PromptStore 路径解析。
3. 删除旧 shot background 内联 Prompt。
4. 删除旧本地 `shot_keyframe_prompt` 拼接逻辑。
5. 删除旧 schema 和兼容属性。
6. 删除旧 smoke 中对旧输出的断言。
7. 检查代码库中不存在旧字段和旧节点职责引用。

## 21. 验证方案

仓库禁止使用 pytest。本重构使用：

- `python -m compileall`
- `scripts/smoke/` 下的定向 smoke
- fake provider 端到端运行
- 小规模真实多模态文本与图片调用
- JSON、Prompt 日志和生成图片人工审计

### 21.1 Prompt 模块 smoke

建议新增：

```text
scripts/smoke/prompt_directory_resolution_smoke.py
scripts/smoke/prompt_renderer_contract_smoke.py
scripts/smoke/final_prompt_audit_log_smoke.py
scripts/smoke/final_prompt_retry_history_smoke.py
scripts/smoke/prompt_content_metadata_lint_smoke.py
```

验证：

- 每个模型节点能解析到唯一目录。
- default 和变体选择正确。
- 缺失变量、未知变量和空 renderer 输出会失败。
- 日志文件内容哈希与 Provider 接收的 Prompt 哈希一致。
- 日志写入后没有隐式 Prompt 拼接。
- 重试历史完整。
- 文件名清理稳定。
- 并发写入不会产生半文件。

### 21.2 shot 合同 smoke

建议新增：

```text
scripts/smoke/clip_to_shots_narrative_angle_smoke.py
scripts/smoke/layout_to_background_prompt_contract_smoke.py
scripts/smoke/shot_background_reuse_smoke.py
scripts/smoke/shot_background_partial_rerun_smoke.py
scripts/smoke/shot_keyframe_prompt_multimodal_refs_smoke.py
scripts/smoke/shot_keyframe_image_generation_contract_smoke.py
scripts/smoke/shot_dependency_invalidation_smoke.py
scripts/smoke/shot_pipeline_fake_e2e_smoke.py
```

验证：

- 每个 shot 恰好一个叙事角度。
- 每个 shot 恰好一个 background。
- 一个 background 可以覆盖多个非连续 shot。
- 相同 background 只调用一次图片 API。
- 背景 Prompt 不要求生成人物。
- 背景 description 可以描述人物关系和镜头朝向。
- keyframe prompt 模型实际收到背景图和人物图。
- keyframe 图片调用的第一张参考图始终是背景。
- 无人物 shot 仍产生独立 keyframe 资产。
- 背景变化使全部关联 keyframe 失效。
- 角色变化不使 background 失效。
- 局部重跑不破坏未选中资产。

### 21.3 真实小样验证

选取：

- 1 个 layout 母版。
- 3–5 个 shot。
- 至少一组可复用背景。
- 至少一组对话正反打。
- 2 个角色。
- 1 个关键道具。

人工检查：

- 背景与母版空间一致。
- 正反打没有被错误复用为同一背景。
- 可复用 shot 确实引用同一个背景文件。
- 关键帧人物身份稳定。
- 关键帧没有重新设计背景机位。
- 每个画面符合对应叙事角度。
- Prompt 日志与实际请求完全一致。

## 22. 验收标准

### 22.1 Prompt 架构

- `prompts/` 下按节点分目录。
- 不存在旧根级节点模板。
- `.md` 只包含裸模完成当前任务所需内容。
- `render.py` 为纯确定性函数。
- node/service 中不再存在可迁移的大段 Prompt。
- Provider 层不再添加隐式业务 Prompt。

### 22.2 Prompt 审计

- 每次模型 API 调用都有对应 `.prompt.txt`。
- 文件位于 `logs/prompts/<资产类型>/`。
- 文件名使用稳定资产名。
- 内容与实际 `prompt` 参数逐字符一致。
- 重试和安全改写历史可追踪。
- Prompt 文件不包含认证信息和媒体 payload。

### 22.3 镜头链路

- 每个 shot 有且只有一个 `narrative_angle`。
- 每个 shot 有且只有一个 background。
- 一个 background 可以服务多个兼容 shot。
- `layout_to_background_prompt` 使用场景母版图进行多模态分析。
- `shot_background_image_generation` 只生成背景。
- `shot_keyframe_prompt` 使用真实背景图和人物图进行多模态分析。
- `shot_keyframe_image_generation` 只生成关键帧。
- background 和 keyframe 不再共享同一个文件路径。

### 22.4 破坏性清理

- 不存在旧 schema alias。
- 不存在旧节点名 alias。
- 不存在旧模板路径 fallback。
- 不存在旧产物读取或迁移逻辑。
- 受影响项目必须通过新链路重新生成。
- 文档和配置只描述新架构。

## 23. 预计主要改动范围

```text
autodrama/src/autodrama/prompts/**
autodrama/src/autodrama/utils/prompts.py
autodrama/src/autodrama/core/schemas.py
autodrama/src/autodrama/core/model_catalog.py
autodrama/src/autodrama/repositories/project_layout.py
autodrama/src/autodrama/services/**
autodrama/src/autodrama/workflows/nodes/shot_asset_nodes.py
autodrama/src/autodrama/workflows/nodes/static_asset_nodes.py
autodrama/src/autodrama/workflows/nodes/storyboard_asset_nodes.py
autodrama/src/autodrama/workflows/nodes/role_nodes.py
autodrama/src/autodrama/workflows/pregen.py
autodrama/src/autodrama/workflows/generation.py
autodrama/src/autodrama/providers/router.py
config.yaml.example
model_catalog.yaml.example
README.md
PREGEN_NODES.md
scripts/smoke/**
```

## 24. 最终执行顺序

```text
冻结新数据合同
→ 重构 PromptStore 和最终 Prompt 日志
→ 迁移并治理全库 Prompt
→ 重构 clip_to_shots
→ 实现 layout_to_background_prompt
→ 实现 shot_background_image_generation
→ 重构 shot_keyframe_prompt
→ 重构 shot_keyframe_image_generation
→ 更新 manifest、配置、CLI 和文档
→ 删除全部旧接口和旧模板
→ 完成 smoke、fake E2E 和真实小样验证
→ 使用新链路重新生成受影响项目
```

