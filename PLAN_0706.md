# Pregen 节点语义统一改造规划

日期：2026-07-06

## 目标

本次改造统一 pregen 默认链路中的节点语义命名，并清理历史旧名。

核心原则：

- 只使用新版节点名，不做旧版本兼容。
- 旧节点名不保留 alias。
- 旧配置 key 不做 fallback。
- 旧产物路径不做 fallback。
- 旧命令参数直接失效。
- `key_vision` 相关节点只改名，不调整默认链路位置。

## 命名冲突结论

`design_key_vision_image` 不改成 `key_vision_image`，统一改成：

```text
key_vision_image_generation
```

这样和其他图像生成节点保持一致：

```text
roleboard_image_generation
prop_image_generation
layout_image_generation
clip_storyboard_image_generation
clip_storyboard_keyframe_generation
```

## 目标改名表

```text
design_key_vision_prompt        -> key_vision_prompt
design_key_vision_image         -> key_vision_image_generation

roleboard_generation            -> roleboard_image_generation

prop_dedupe                     -> prop_finalize
layout_dedupe_review            -> layout_finalize

storyboard_prompt               -> clip_storyboard_prompt
storyboard_generation           -> clip_storyboard_image_generation
storyboard_keyframe_generation  -> clip_storyboard_keyframe_generation
```

不保留旧名作为兼容别名。默认链路、配置、文档、产物、日志、`source_node`、`provider_binding_node`、smoke 全部只使用新名。

## 目标默认顺序

`key_vision` 相关节点保持当前默认链路位置，只改名，不挪到角色、道具、场景之后。

```text
script_import
script_detail_expand
script_novel_extract

key_vision_prompt
key_vision_image_generation

role_extract_primary
role_extract_functional
role_finalize
roleboard_prompt
roleboard_image_generation

prop_extract
prop_finalize
prop_prompt
prop_image_generation

layout_extract
layout_finalize
layout_prompt
layout_image_generation

clip_segment
clip_prompt
clip_storyboard_prompt
clip_storyboard_image_generation
clip_storyboard_keyframe_generation
clip_manifest_generation
```

当前代码里 `roleboard_generation` 会读取 key vision，并在缺少主视觉图时要求先运行 `design_key_vision_image`。改造后仍保留这条依赖，只把提示和引用改成新版节点名：

```text
roleboard_image_generation requires the key vision image; run key_vision_image_generation first
```

## 硬切策略

本次不做兼容迁移逻辑。

不要新增或保留：

```text
PREGEN_NODE_ALIASES
PREGEN_ONLY_ALIASES 中的新旧节点映射
old_name -> new_name resolve 逻辑
旧配置 key fallback
旧产物路径 fallback
旧 prompt 模板 fallback
旧参数名 fallback
```

旧命令例如：

```text
run pregen --only storyboard_generation
run pregen --until design_key_vision_image
```

应该直接报未知节点名错误。

已有项目如果只存在旧产物，例如：

```text
assets/json/nodes/roleboard_generation.json
assets/json/nodes/prop_dedupe.json
assets/json/nodes/storyboard_generation.json
```

新代码不会再读取它们。处理方式：

- 接受断点重跑，使用新节点名重新生成。
- 或提供一次性离线迁移脚本，把旧产物文件重命名为新产物文件。

一次性迁移脚本不属于运行时兼容逻辑。迁移完成后，代码仍然只认新名。

## 代码改动范围

### 1. 工作流节点注册和默认顺序

重点文件：

```text
autodrama/src/autodrama/workflows/pregen.py
autodrama/src/autodrama/workflows/nodes/__init__.py
```

要做：

- `PREGEN_NODES` 使用新版默认顺序。
- `AVAILABLE_PREGEN_NODE_NAMES` 只包含新节点名和仍然有效的手动节点名。
- `EPISODE_SCOPED_PREGEN_ONLY_NODES` 中的旧节点名全部替换为新节点名。
- `--only old_name` 不再可用。
- `--until old_name` 不再可用。
- 依赖检查、错误提示、日志输出全部只显示新名。
- smoke 中默认链路只检查新顺序。

当前 `build_pregen_nodes()` 结构大体可以保留：

```text
SCRIPT_NODE_NAMES
DIRECTOR_NODE_NAMES
DEFAULT_PREGEN_ROLE_NODE_NAMES
STATIC_ASSET_AND_CLIP_NODE_NAMES
STORYBOARD_ASSET_NODE_NAMES
```

但需要把其中的旧节点名替换为新节点名。`DIRECTOR_NODE_NAMES` 的位置不变，因此 `key_vision_prompt` 和 `key_vision_image_generation` 仍然位于角色节点之前。

### 2. Director / key vision 节点改名

重点文件：

```text
autodrama/src/autodrama/workflows/nodes/director_nodes.py
```

要做：

- `design_key_vision_prompt` -> `key_vision_prompt`
- `design_key_vision_image` -> `key_vision_image_generation`
- `DIRECTOR_NODE_NAMES` 改成新名。
- 节点类 `.name` 改成新名。
- 日志里的 `node=design_key_vision_*` 改成新名。
- 错误提示里的旧命令改成新命令。
- `reference_source="design_key_vision_image"` 改成 `reference_source="key_vision_image_generation"`。

内部业务 metadata 可以按语义保留，例如：

```text
key_vision_prompt
key_vision_asset
key_vision_asset_path
key_vision_generation_path
```

这些不是旧节点名，可以不改。

### 3. 角色板节点改名

重点文件：

```text
autodrama/src/autodrama/workflows/nodes/role_nodes.py
autodrama/src/autodrama/workflows/nodes/static_asset_nodes.py
autodrama/src/autodrama/workflows/pregen.py
```

要做：

- `roleboard_generation` -> `roleboard_image_generation`
- `RoleboardGenerationNode.name` 改成 `roleboard_image_generation`。
- `router.image("role", node_name="roleboard_generation")` 改成新版节点名。
- `_node_params("roleboard_generation")` 改成新版节点名。
- 日志、错误提示、命令示例全部改成新版节点名。
- `source_node="roleboard_generation"` 改成 `source_node="roleboard_image_generation"`。

保留 roleboard 对 key vision 的依赖：

```text
key_vision_reference_ref(...)
```

但提示文字要改成：

```text
roleboard_image_generation requires the key vision image; run key_vision_image_generation first
```

### 4. 道具和场景 finalize 节点改名

重点文件：

```text
autodrama/src/autodrama/workflows/nodes/static_asset_nodes.py
```

要做：

- `prop_dedupe` -> `prop_finalize`
- `layout_dedupe_review` -> `layout_finalize`
- `STATIC_ASSET_NODE_NAMES` 改成新名。
- `build_pregen_nodes()` 中插入 `clip_segment` 的判断从 `layout_dedupe_review` 改成 `layout_finalize`。
- `STATIC_ASSET_AND_CLIP_NODE_NAMES` 中插入 `clip_segment` 的判断同步改成 `layout_finalize`。

内部引用同步改名：

```text
load_prop_dedupe_output       -> load_prop_finalize_output
load_layout_dedupe_output     -> load_layout_finalize_output
source_prop_dedupe_path       -> source_prop_finalize_path
source_layout_dedupe_path     -> source_layout_finalize_path
source="prop_dedupe"          -> source="prop_finalize"
source="layout_dedupe_review" -> source="layout_finalize"
```

业务字段如果表达的是生成内容，而不是节点名，可以保留，例如：

```text
generated_prop_intro
generated_layout_intro
```

### 5. Storyboard / clip 节点改名

重点文件：

```text
autodrama/src/autodrama/workflows/nodes/storyboard_asset_nodes.py
```

要做：

- `storyboard_prompt` -> `clip_storyboard_prompt`
- `storyboard_generation` -> `clip_storyboard_image_generation`
- `storyboard_keyframe_generation` -> `clip_storyboard_keyframe_generation`
- `STORYBOARD_ASSET_NODE_NAMES` 改成新名。
- `STORYBOARD_IMAGE_PROVIDER_NODE_NAME` 重新评估并统一为 `clip_storyboard_image_generation`。

建议直接把 provider binding 统一成：

```text
clip_storyboard_image_generation
```

不要再保留额外的历史名称：

```text
storyboard_sheet_generation
storyboard_generation
```

除非 `storyboard_sheet_generation` 是完全不同的节点。如果只是历史命名残留，应一并删除或改名。

内部函数、变量、错误提示同步替换：

```text
load_storyboard_prompt_output
load_storyboard_generation_output
load_storyboard_keyframe_output
```

改成：

```text
load_clip_storyboard_prompt_output
load_clip_storyboard_image_generation_output
load_clip_storyboard_keyframe_generation_output
```

错误提示改成：

```text
run pregen through clip_storyboard_prompt first
missing clip_storyboard_image_generation image
missing clip_storyboard_keyframe_generation output
```

### 6. 配置文件改名

重点文件：

```text
config.yaml
config.yaml.example
model_catalog.yaml.example
```

`config.yaml` 可能是 ignored，但本地也要一起改，否则本地运行会直接断。

配置节点改成：

```yaml
key_vision_prompt:
  ...

key_vision_image_generation:
  model: aibox:gpt-image-2-guan
  params:
    ...

roleboard_image_generation:
  model: aibox:gpt-image-2-guan
  params:
    roleboard_image_generation_concurrency: 3
    ...

prop_finalize:
  model: rightcode:gpt-5.6-terra
  params:
    ...

layout_finalize:
  model: rightcode:gpt-5.6-terra
  params:
    ...

clip_storyboard_prompt:
  model: rightcode:gpt-5.6-terra
  params:
    ...

clip_storyboard_image_generation:
  model: aibox:gpt-image-2-guan
  params:
    clip_storyboard_image_generation_concurrency: 3
    ...

clip_storyboard_keyframe_generation:
  model: aibox:gpt-image-2-guan
  params:
    clip_storyboard_keyframe_generation_concurrency: 3
    ...
```

参数名也直接改成新名，不保留旧参数 fallback。

删除旧参数读取：

```text
roleboard_generation_concurrency
storyboard_generation_concurrency
storyboard_keyframe_generation_concurrency
```

只读取新参数：

```text
roleboard_image_generation_concurrency
clip_storyboard_image_generation_concurrency
clip_storyboard_keyframe_generation_concurrency
```

### 7. Prompt 模板与日志路径

重点目录可能包括：

```text
autodrama/src/autodrama/prompts/
autodrama/src/autodrama/workflows/dynamic_assets.py
autodrama/src/autodrama/logging.py
```

要做：

```text
design_key_vision_prompt.md -> key_vision_prompt.md
storyboard_prompt.md        -> clip_storyboard_prompt.md
```

如果还有其他按节点名查找的 prompt 模板，也同步改名。

不要做：

```text
不要先找新模板，再 fallback 旧模板。
不要保留旧模板查找路径。
不要在日志目录里继续写旧节点名。
```

新的 prompt 日志路径应该只出现新节点名。

### 8. 产物路径硬切新名

节点输出通常会落到：

```text
assets/json/nodes/<node_name>.json
```

改名后新产物路径变成：

```text
assets/json/nodes/key_vision_prompt.json
assets/json/nodes/key_vision_image_generation.json
assets/json/nodes/roleboard_image_generation.json
assets/json/nodes/prop_finalize.json
assets/json/nodes/layout_finalize.json
assets/json/nodes/clip_storyboard_prompt.json
assets/json/nodes/clip_storyboard_image_generation.json
assets/json/nodes/clip_storyboard_keyframe_generation.json
```

不要做读取 fallback：

```text
不要读 key_vision_image_generation 失败后尝试 design_key_vision_image
不要读 roleboard_image_generation 失败后尝试 roleboard_generation
不要读 prop_finalize 失败后尝试 prop_dedupe
不要读 layout_finalize 失败后尝试 layout_dedupe_review
不要读 clip_storyboard_prompt 失败后尝试 storyboard_prompt
不要读 clip_storyboard_image_generation 失败后尝试 storyboard_generation
不要读 clip_storyboard_keyframe_generation 失败后尝试 storyboard_keyframe_generation
```

### 9. 下游引用全量替换

重点全局搜索这些旧名：

```text
design_key_vision_prompt
design_key_vision_image
roleboard_generation
prop_dedupe
layout_dedupe_review
storyboard_prompt
storyboard_generation
storyboard_keyframe_generation
storyboard_sheet_generation
```

特别注意这些位置：

```text
source_node
provider_binding_node
node_output_path
run pregen --only ...
run pregen --until ...
episode-scoped rerun supported node list
clip_manifest_generation 读取 storyboard/keyframe 的地方
roleboard 作为视频参考图 source 的地方
配置节点名
模型 catalog 节点名
prompt 模板名
日志目录名
smoke 断言
README 示例
```

`clip_manifest_generation` 里如果会把 storyboard、关键帧、roleboard 等资产写入 shot manifest，manifest 里的 `source_node` 也必须改成新名：

```text
roleboard_image_generation
clip_storyboard_image_generation
clip_storyboard_keyframe_generation
```

不要让 manifest 继续出现：

```text
roleboard_generation
storyboard_generation
storyboard_keyframe_generation
```

### 10. 文档更新

重点文件：

```text
PREGEN_NODES.md
README.md
autodrama/README.md
FirstEndFramePlan.md
```

要更新：

- 默认顺序
- 节点说明
- 示例命令
- `--only` 示例
- `--until` 示例
- 配置示例
- 产物路径说明
- `source_node` 示例

示例命令改成：

```text
run\start.cmd --config config.yaml --project <project_id> --until key_vision_image_generation
run\start.cmd --config config.yaml --project <project_id> --only clip_storyboard_keyframe_generation --episodes 1
run\start.cmd --config config.yaml --project <project_id> --only roleboard_image_generation
```

删除旧节点兼容说明。

可以明确写：

```text
旧节点名已移除，请使用新版节点名。
已有旧产物需要重新生成，或使用一次性迁移脚本改名。
```

### 11. Smoke / 合约检查更新

重点文件：

```text
scripts/smoke/pregen_default_chain_contract_smoke.py
scripts/smoke/clip_segment_contract_smoke.py
scripts/smoke/aibox_image_nodes_guan_smoke.py
scripts/smoke/aibox_key_vision_params_smoke.py
scripts/smoke/config_keyframe_node_smoke.py
```

要做：

- 默认链路顺序改成新版。
- `aibox_image_nodes_guan_smoke.py` 的节点名改成新版。
- `aibox_key_vision_params_smoke.py` 读取 `key_vision_image_generation`。
- `config_keyframe_node_smoke.py` 读取 `clip_storyboard_keyframe_generation`。
- `clip_segment_contract_smoke.py` 中如有 storyboard 依赖名，一并改新。

不要新增 alias smoke。

新增或更新一个“旧名已清理” smoke：

```text
AVAILABLE_PREGEN_NODE_NAMES 不包含旧名。
PREGEN_NODES 不包含旧名。
默认链路不包含旧名。
配置示例不包含旧节点 key。
核心 manifest/source_node 不包含旧名。
```

建议检查这些旧名不存在：

```text
design_key_vision_prompt
design_key_vision_image
roleboard_generation
prop_dedupe
layout_dedupe_review
storyboard_prompt
storyboard_generation
storyboard_keyframe_generation
```

## 验证计划

按仓库规则，不跑 pytest。

先跑编译检查：

```text
D:/miniforge3/envs/autodrama/python.exe -m compileall autodrama/src scripts/smoke
```

再跑 smoke：

```text
D:/miniforge3/envs/autodrama/python.exe scripts/smoke/pregen_default_chain_contract_smoke.py
D:/miniforge3/envs/autodrama/python.exe scripts/smoke/aibox_image_nodes_guan_smoke.py
D:/miniforge3/envs/autodrama/python.exe scripts/smoke/aibox_key_vision_params_smoke.py
D:/miniforge3/envs/autodrama/python.exe scripts/smoke/clip_segment_contract_smoke.py
D:/miniforge3/envs/autodrama/python.exe scripts/smoke/config_keyframe_node_smoke.py
```

最后跑：

```text
git diff --check
```

如果要验证真实链路，可以跑较小范围的 CLI smoke：

```text
D:/miniforge3/envs/autodrama/python.exe -m autodrama.cli run pregen --config config.yaml --project <project_id> --only key_vision_prompt --force
D:/miniforge3/envs/autodrama/python.exe -m autodrama.cli run pregen --config config.yaml --project <project_id> --only clip_storyboard_prompt --episodes 1 --force
```

是否运行图像生成节点要看是否会消耗外部模型额度。

## 实施顺序建议

1. 改 `director_nodes.py` 中 key vision 节点名，但不调整其默认链路位置。
2. 改 `roleboard_generation` 为 `roleboard_image_generation`，保留对 key vision 的前置依赖。
3. 改 `prop_dedupe`、`layout_dedupe_review` 为 `prop_finalize`、`layout_finalize`。
4. 改 storyboard 相关节点为 `clip_storyboard_*`。
5. 更新 `PREGEN_NODES`、`AVAILABLE_PREGEN_NODE_NAMES`、`EPISODE_SCOPED_PREGEN_ONLY_NODES`。
6. 删除旧名 alias、resolve、fallback 逻辑，如果当前或后续分支里已经存在。
7. 改配置读取 key 和参数名，只认新名。
8. 改 `config.yaml`、`config.yaml.example`、`model_catalog.yaml.example`。
9. 改 prompt 模板名和 prompt 查找逻辑，只查新模板。
10. 改产物读取路径，只读新节点名产物。
11. 改 `source_node`、`provider_binding_node`、manifest 写入。
12. 全局搜索旧名，逐个清理。
13. 改 smoke，新增或更新“旧名不存在”检查。
14. 改文档。
15. 跑 `compileall`、smoke、`git diff --check`。

## 最终验收标准

- 默认 pregen 链路只包含新版节点名。
- `key_vision_prompt` 和 `key_vision_image_generation` 仍位于角色节点之前。
- `roleboard_image_generation` 仍依赖 `key_vision_image_generation`。
- `AVAILABLE_PREGEN_NODE_NAMES` 不包含旧节点名。
- `EPISODE_SCOPED_PREGEN_ONLY_NODES` 不包含旧节点名。
- 配置示例不包含旧节点名。
- 产物读取不 fallback 到旧节点名。
- prompt 模板查找不 fallback 到旧节点名。
- manifest 中的 `source_node` 不出现旧节点名。
- 文档和示例命令不出现旧节点名，除非是在说明“旧名已移除”。
- 非 pytest 验证通过。
