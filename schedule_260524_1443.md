# AutoDrama 音色画像与 voice_select 规划

时间：2026-05-24 14:43  
范围：全局音色样例库、Omni 音色画像、项目内 `voice_select` 节点、角色声音生成链路收敛  
原则：音色选择不能只靠官方名称和 LLM 文字猜测。官方音色应先转化为“可听、可描述、可检索、可 judge”的全局资产；项目内只做角色级选择和绑定，避免重复生成全量音色样例。

## 1. 背景问题

当前角色声音链路里，`role_design` 会结合可用音色列表尝试选择 `voice_name`、`voice_type` 和 `voice_resource_id`。这有明显风险：

- 官方音色名和实际听感不一定一致。
- LLM 只看文字描述，容易把“名字合适”的音色选成“实际不适合”的音色。
- 同一个角色后续所有对白都会复用同一音色，一旦选错，影响范围很大。
- 人物音色资产本身是一次性的，声音合成成本相对较低，因此为官方音色做一次性样例库是可接受的。

目标是把选择链路改成：

```text
官方音色列表
-> 为每个音色生成固定多情绪样例
-> Omni 听音频，生成音色的真实听感文字画像
-> 项目内根据人物设计和音色画像筛 top 5
-> Omni 听 top 5 样例，选择最适合的音色
-> role_voice_generation 使用选中音色合成人物 sample_text 音频
```

## 2. 总体架构

### 2.1 全局音色库构建

全局音色库是跨项目复用资产，不应放在单个项目的 `outputs/<project_id>` 目录中。

建议目录：

```text
.assets/voice_catalog/volcengine/seed-tts-2.0/
  manifest.json
  samples/
    zh_female_xiaohe_uranus_bigtts/
      normal.mp3
      angry.mp3
      sad.mp3
      happy.mp3
      low.mp3
      manifest.json
    zh_male_m191_uranus_bigtts/
      normal.mp3
      angry.mp3
      sad.mp3
      happy.mp3
      low.mp3
      manifest.json
```

全局流程：

```text
seed_tts_speakers.json
-> synthesize catalog samples
-> omni profile generation
-> manifest.json
```

### 2.2 项目内音色选择

项目内新增 `voice_select` 节点：

```text
role_design
-> voice_select
-> role_voice_generation
```

职责切分：

- `role_design` 负责人物介绍、性格、声音需求、各情绪 `sample_text`。
- `voice_select` 负责选择具体官方音色。
- `role_voice_generation` 负责用选中的官方音色合成人物专属样例音频。

## 3. 中文标签与官方 voice_type 映射

用户侧希望“hash 标签是音色的中文名字，在实际执行 API 的时候 hash 成官方标准 `hash_type`”。这里建议收敛成更明确的工程语义：

```text
面向人和 LLM 的标签：中文音色名 / voice_label
实际 API 执行字段：官方 voice_type
可选内部稳定键：voice_catalog_key
```

不要真的用中文名做不可逆 hash 后猜官方 ID。原因：

- 中文名可能重复。
- 官方可能改中文名但保留 `voice_type`。
- 不同 provider 可能存在同名音色。
- `voice_type` 才是 provider API 真正需要的 speaker 标识。

建议数据字段：

```json
{
  "voice_label": "小何 2.0",
  "voice_type": "zh_female_xiaohe_uranus_bigtts",
  "voice_resource_id": "seed-tts-2.0",
  "voice_catalog_key": "volcengine:seed-tts-2.0:zh_female_xiaohe_uranus_bigtts"
}
```

执行规则：

```text
prompt / UI / node output summary:
  优先展示 voice_label

catalog lookup / state binding / API payload:
  必须使用 voice_type

文件路径:
  可以使用 voice_type，避免中文名冲突和非法路径字符
```

如果后续代码中仍使用 `hash_type` 这个名字，应把它定义为：

```text
hash_type = provider 官方标准 voice_type / speaker id
```

而不是从中文名再次计算出的 hash。更推荐直接命名为 `voice_type`，避免误解。

当中文名重复时，展示层可写：

```text
小何 2.0 / zh_female_xiaohe_uranus_bigtts
```

但落盘和 API 始终以 `voice_type` 为准。

## 4. 全局音色样例设计

固定生成五类样例：

```text
normal
angry
sad
happy
low
```

样例文本建议固定：

```json
{
  "normal": "今天的事先这样吧，等你想清楚了，我们再谈。",
  "angry": "我已经说过很多遍了，不要再这样做。",
  "sad": "我只是没想到，事情最后会变成这样。",
  "happy": "太好了，我就知道你一定能做到。",
  "low": "这件事不要声张，先听我说完。"
}
```

说明：

- `normal` 是基础听感。
- `angry`、`sad`、`happy` 用于测试常见情绪表现。
- `low` 用于测试低沉、压迫、私密、威胁、稳重感。
- `low` 不等于 `whisper`。`whisper` 是耳语，`low` 是压低声线或低沉表达。

样例生成缓存 key：

```text
provider
model/resource_id
voice_type
emotion
sample_text
emotion_params
response_format
sample_rate
```

只要 key 未变且样例文件存在，就跳过，除非显式 `--force`。

## 5. Omni 音色画像

全局样例生成后，用 Qwen3.5-Omni-Plus 之类能听音频的模型，为每个音色生成结构化画像。

输入：

```text
同一个 voice_type 的 normal / angry / sad / happy / low 五段音频
官方 metadata：name, voice_type, gender, language, scene, tags, supported_emotions
```

输出示例：

```json
{
  "summary": "成熟、清晰、温和，情绪起伏稳定，适合理性或照顾型女性角色。",
  "gender_presentation": "female",
  "age_impression": "young_adult_to_adult",
  "texture": ["clear", "warm", "stable"],
  "performance_style": ["natural", "restrained"],
  "strengths": ["普通对白自然", "悲伤情绪不过度"],
  "weaknesses": ["愤怒爆发力度较弱"],
  "best_role_types": ["母亲", "医生", "理性女性", "温和长辈"],
  "avoid_role_types": ["强压迫反派", "童声", "极端疯癫角色"],
  "emotion_quality": {
    "normal": 8.5,
    "angry": 6.8,
    "sad": 8.2,
    "happy": 7.4,
    "low": 7.9
  }
}
```

画像 prompt 关键约束：

```text
你不是选择角色，而是在描述音色本身。
忽略样例台词内容。
不要把官方名称当成事实，以实际听感为准。
输出适合检索和匹配的结构化 JSON。
```

## 6. 项目内 voice_select 逻辑

`voice_select` 节点输入：

- `state.roles`
- 每个角色的 `intro`
- `personality`
- `voice_summary`
- `role.audio[*].desc`
- `role.audio[*].sample_text`
- 全局 voice catalog `manifest.json`

选择分两阶段。

### 6.1 文本初筛 top 5

使用角色设计和全局音色画像文本，从全部音色中选出 5 个候选。

输入：

```text
角色设定
角色声音需求
全局音色画像 summary/tags/best_role_types/avoid_role_types/emotion_quality
```

输出：

```json
{
  "candidates": [
    {
      "voice_label": "小何 2.0",
      "voice_type": "zh_female_xiaohe_uranus_bigtts",
      "score": 8.8,
      "reason": "成熟清晰，情绪稳定，适合温和但有控制感的母亲角色。"
    }
  ]
}
```

这里可以用普通文本 LLM，也可以用 Omni 的文本能力。关键是不听全部音频，避免成本和上下文失控。

### 6.2 音频终选

把 top 5 候选的五情绪音频交给 Omni 终选。

输入：

```text
角色设定
top 5 候选音色画像
top 5 * 5 emotion samples
```

输出：

```json
{
  "selected_voice_type": "zh_female_xiaohe_uranus_bigtts",
  "selected_voice_label": "小何 2.0",
  "selected_reason": "普通对白自然，低沉状态有克制压迫感，悲伤不夸张，适合韩母。",
  "ranked_candidates": []
}
```

judge prompt 关键约束：

```text
你不是判断台词内容，而是判断音色是否适合角色。
优先判断年龄感、性别呈现、身份匹配、性格匹配、情绪表现稳定性、短剧对白自然度。
警惕过强广告腔、客服腔、播音腔、动漫腔。
如果官方描述和实际听感冲突，以实际听感为准。
```

## 7. Schema 规划

建议新增到独立文件：

```text
autodrama/src/autodrama/core/voice_catalog.py
```

而不是继续膨胀 `schemas.py`。

核心 schema：

```python
class VoiceCatalogSampleItem(BaseModel):
    emotion: str
    sample_text: str
    asset_path: str
    sample_hash: str
    provider: str
    model: str
    voice_type: str
    voice_resource_id: str | None = None
    audio_params: dict[str, Any] = Field(default_factory=dict)

class VoiceCatalogProfile(BaseModel):
    summary: str
    gender_presentation: str | None = None
    age_impression: str | None = None
    texture: list[str] = Field(default_factory=list)
    performance_style: list[str] = Field(default_factory=list)
    strengths: list[str] = Field(default_factory=list)
    weaknesses: list[str] = Field(default_factory=list)
    best_role_types: list[str] = Field(default_factory=list)
    avoid_role_types: list[str] = Field(default_factory=list)
    emotion_quality: dict[str, float] = Field(default_factory=dict)

class VoiceCatalogVoiceItem(BaseModel):
    voice_label: str
    voice_type: str
    voice_resource_id: str | None = None
    voice_model_family: str | None = None
    voice_catalog_key: str
    official: dict[str, Any] = Field(default_factory=dict)
    samples: dict[str, VoiceCatalogSampleItem] = Field(default_factory=dict)
    omni_profile: VoiceCatalogProfile | None = None
    profile_hash: str | None = None

class VoiceCatalogManifest(BaseModel):
    schema_version: int = 1
    catalog_version: str
    provider: str
    model: str
    sample_emotions: list[str]
    voices: list[VoiceCatalogVoiceItem]
```

项目内选择输出：

```python
class VoiceCandidateItem(BaseModel):
    voice_label: str
    voice_type: str
    voice_resource_id: str | None = None
    voice_model_family: str | None = None
    voice_catalog_key: str
    score: float | None = None
    reason: str
    profile_summary: str | None = None
    sample_paths: dict[str, str] = Field(default_factory=dict)

class RoleVoiceSelectionItem(BaseModel):
    role_id: str
    role_name: str
    selected_voice_label: str
    selected_voice_type: str
    selected_voice_resource_id: str | None = None
    selected_voice_model_family: str | None = None
    selected_voice_catalog_key: str
    selected_reason: str
    selection_source: Literal["manual_override", "cache", "omni_judge"] | str = "omni_judge"
    top_candidates: list[VoiceCandidateItem] = Field(default_factory=list)
    role_design_hash: str
    catalog_version: str
    catalog_hash: str
    provider: str
    model: str
    request_id: str | None = None
    usage: dict[str, Any] = Field(default_factory=dict)
    raw_response: dict[str, Any] = Field(default_factory=dict)

class VoiceSelectOutput(BaseModel):
    selected_voices: list[RoleVoiceSelectionItem]
```

## 8. Provider 与 Router 规划

当前 `TextLLM` 只有纯文本 `generate_json`。需要新增可听音频并输出 JSON 的能力：

```python
class AudioJudgeLLM(Protocol):
    name: str

    async def judge_audio_json(
        self,
        prompt: str,
        schema: type[T],
        *,
        refs: list[AssetRef],
        temperature: float = 0.2,
        metadata: dict[str, Any] | None = None,
    ) -> T:
        ...
```

新增 provider：

```text
autodrama/src/autodrama/providers/aliyun/omni/qwen_omni.py
```

新增 router capability：

```python
router.judge("voice_catalog_profile")
router.judge("voice_select")
```

配置示例：

```yaml
providers:
  aliyun_omni:
    base_url: https://dashscope.aliyuncs.com
    api_key_env: ALIYUN_API_KEY
    models:
      audio_judge: qwen3.5-omni-plus
    options:
      temperature: 0.2

routing:
  judge:
    voice_catalog_profile: aliyun_omni
    voice_select: aliyun_omni
```

## 9. Repository / Service 规划

新增：

```text
autodrama/src/autodrama/repositories/voice_catalog_repo.py
autodrama/src/autodrama/services/voice_catalog_service.py
```

`VoiceCatalogRepository` 职责：

- 解析 catalog root。
- 读写 `manifest.json`。
- 根据 provider/model/voice_type/emotion 生成样例路径。
- 计算 catalog hash。
- 根据 `voice_label` 和 `voice_type` 做 lookup。
- 检测中文名重复。

`VoiceCatalogService` 职责：

- 读取官方 speaker catalog。
- 调 speech provider 生成样例。
- 调 Omni provider 生成听感画像。
- 维护幂等缓存。
- 输出可供 `voice_select` 使用的 compact candidates。

## 10. Workflow 节点规划

当前 voice node 实际只注册：

```text
role_voice_generation
```

应改为：

```text
voice_select
role_voice_generation
```

新增：

```text
VoiceSelectNode
```

位置可先放：

```text
autodrama/src/autodrama/workflows/nodes/voice_nodes.py
```

如果后续代码变大，再拆：

```text
autodrama/src/autodrama/workflows/nodes/voice_select_node.py
```

`VoiceSelectNode.run()` 流程：

```text
1. hydrate role design files
2. load voice catalog manifest
3. load existing voice_select.json
4. resolve active episode roles
5. manual override check
6. valid cache check
7. shortlist top 5
8. Omni audio judge final selection
9. bind selected voice to Role fields
10. save voice_select.json
```

绑定字段复用现有 `Role` 字段：

```python
role.voice_name = selected_voice_label
role.voice_type = selected_voice_type
role.voice_resource_id = selected_voice_resource_id
role.voice_model_family = selected_voice_model_family
role.voice_selection_reason = selected_reason
```

后续 `role_voice_generation` 已经优先使用 `role.voice_type`，因此改动面较小。

## 11. role_design 职责调整

`role_design` 后续不应继续负责最终 `voice_type` 选择。

保留：

```text
voice_summary
audio[emotion].desc
audio[emotion].sample_text
```

弱化或置空：

```text
voice_name
voice_type
voice_resource_id
voice_selection_reason
```

prompt 规则应改为：

```text
不要选择具体 provider voice_type。
只描述角色需要什么样的声音、语气、年龄感、性格感，并生成可直接朗读的 sample_text。
具体音色由 voice_select 节点根据全局音色画像和实际试听样例决定。
```

短期兼容可以继续保留 schema 字段，但 prompt 不再要求填写具体音色。

## 12. role_voice_generation 接入

`role_voice_generation` 不需要负责选音色，只负责合成。

执行前提：

```text
role.voice_type 已由 voice_select 写入
role.audio[*].sample_text 已由 role_design 写入
```

执行时：

```text
voice = role.voice_type
resource_id = role.voice_resource_id or provider.resolve_voice_resource_id(voice)
text = audio.sample_text
emotion_params = provider.resolve_emotion_plan(audio.emotion)
```

最终输出仍保存：

```text
assets/json/nodes/role_voice_generation.json
assets/audios/role_voices/*.mp3
```

## 13. 缓存与 rerun 规则

`voice_select` 缓存 key：

```text
role_id
role_design_hash
catalog_version
catalog_hash
selection_model
selection_prompt_version
```

复用条件：

```text
existing role_id 命中
role_design_hash 未变
catalog_version 未变
catalog_hash 未变
selected_voice_type 仍存在于 catalog
未 --force
```

`--episodes` 行为：

```text
voice_select --episodes episode_002
```

只处理目标 episode 涉及角色，并 merge 旧的 `voice_select.json`，不能覆盖其他 episode 的角色选择。

`--force` 行为：

```text
--force voice_select
```

只重选角色音色，不自动重生成全局 catalog 样例。

全局 catalog 的 force 应单独控制：

```text
voice-catalog build --force-samples
voice-catalog build --force-profiles
```

## 14. 手工覆盖优先级

必须保留人工覆盖，优先级：

```text
config.yaml role_speakers[role_id]
> config.yaml role_speakers[role_name]
> existing valid voice_select cache
> new voice_select
> provider fallback default speaker
```

如果命中手工覆盖，`voice_select.json` 仍应写记录：

```json
{
  "selection_source": "manual_override",
  "selected_voice_label": "小何 2.0",
  "selected_voice_type": "zh_female_xiaohe_uranus_bigtts"
}
```

这样后续排查可以知道为什么没有走 Omni judge。

## 15. CLI / 脚本规划

新增全局 catalog 命令：

```powershell
D:/miniforge3/envs/autodrama/python.exe -m autodrama voice-catalog build --provider volcengine
D:/miniforge3/envs/autodrama/python.exe -m autodrama voice-catalog build --provider volcengine --force-samples
D:/miniforge3/envs/autodrama/python.exe -m autodrama voice-catalog build --provider volcengine --force-profiles
D:/miniforge3/envs/autodrama/python.exe -m autodrama voice-catalog inspect --provider volcengine
```

更新 pregen：

```powershell
D:/miniforge3/envs/autodrama/python.exe -m autodrama pregen --only voice_select
D:/miniforge3/envs/autodrama/python.exe -m autodrama pregen --only voice_select --episodes episode_001
D:/miniforge3/envs/autodrama/python.exe -m autodrama pregen --only role_voice_generation
```

更新：

```text
scripts/run_pregen.sh
run/rerun_nodes.cmd
README.md 或对应开发说明
```

## 16. Prompt 文件规划

新增：

```text
autodrama/src/autodrama/prompts/voice_catalog_profile.md
autodrama/src/autodrama/prompts/voice_select_shortlist.md
autodrama/src/autodrama/prompts/voice_select_audio_judge.md
```

修改：

```text
autodrama/src/autodrama/prompts/role_design.md
```

目标：

- `voice_catalog_profile.md`：听一个音色的五情绪样例，生成音色画像。
- `voice_select_shortlist.md`：根据角色设定和全部音色画像选 top 5。
- `voice_select_audio_judge.md`：听 top 5 样例，终选一个音色。
- `role_design.md`：不再要求选择具体 `voice_type`，只生成角色声音需求和 sample_text。

## 17. 实施阶段

### 阶段 1：schema 和 repository

- 新增 voice catalog schema。
- 新增 voice catalog repo。
- 加 fake manifest，先不接真实 provider。
- 验证 catalog lookup、中文名重复检测、voice_type lookup。

### 阶段 2：voice_select 节点骨架

- 新增 `VoiceSelectNode`。
- 接入 workflow node list。
- 支持 `--episodes`。
- 支持读取 fake catalog。
- 支持写 `voice_select.json`。
- 支持把选择结果绑定到 `Role`。

### 阶段 3：role_voice_generation 使用 voice_select

- 确认 `role_voice_generation` 使用 `role.voice_type`。
- 加 smoke 验证：`voice_select` 写入的官方 `voice_type` 会进入 synthesize API payload。
- 保留 provider fallback。

### 阶段 4：全局样例生成

- 新增 `voice-catalog build`。
- 读取 `seed_tts_speakers.json`。
- 为每个 speaker 生成 normal/angry/sad/happy/low。
- 写样例文件和 manifest。
- 支持断点续跑。

### 阶段 5：Omni 音色画像

- 新增 `AudioJudgeLLM` 协议。
- 新增 Qwen Omni provider。
- 接 `voice_catalog_profile.md`。
- 为每个音色写 `omni_profile`。
- 支持 profile hash 和断点续跑。

### 阶段 6：Omni 终选

- `voice_select` 从 fake judge 切到真实 judge。
- 实现 top 5 音频 refs。
- 写 ranked candidates 和最终选择理由。

### 阶段 7：role_design 清理

- 修改 `role_design.md`。
- 不再让 `role_design` 选择具体 `voice_type`。
- 只保留声音需求和 sample_text。
- 兼容已有字段，但新流程不依赖它们。

## 18. 验证计划

不使用 pytest。新增 smoke：

```text
scripts/smoke/voice_catalog_manifest_smoke.py
scripts/smoke/voice_catalog_label_lookup_smoke.py
scripts/smoke/voice_select_cache_smoke.py
scripts/smoke/voice_select_episode_scoping_smoke.py
scripts/smoke/role_voice_generation_uses_voice_select_smoke.py
scripts/smoke/voice_select_manual_override_smoke.py
```

验证命令：

```powershell
D:/miniforge3/envs/autodrama/python.exe -m compileall autodrama/src scripts/smoke
D:/miniforge3/envs/autodrama/python.exe scripts/smoke/voice_catalog_manifest_smoke.py
D:/miniforge3/envs/autodrama/python.exe scripts/smoke/voice_catalog_label_lookup_smoke.py
D:/miniforge3/envs/autodrama/python.exe scripts/smoke/voice_select_cache_smoke.py
D:/miniforge3/envs/autodrama/python.exe scripts/smoke/voice_select_episode_scoping_smoke.py
D:/miniforge3/envs/autodrama/python.exe scripts/smoke/role_voice_generation_uses_voice_select_smoke.py
D:/miniforge3/envs/autodrama/python.exe scripts/smoke/voice_select_manual_override_smoke.py
```

## 19. 风险与约束

- Qwen3.5-Omni-Plus 的音频输入 API 细节需要单独确认，不能假设和纯文本 OpenAI-compatible endpoint 完全一致。
- 一次性处理 448 个音色需要断点续跑和并发限制。
- top 5 音频终选的输入体积可能较大，需要控制音频长度。
- 中文名不能作为唯一键，只能作为展示标签。
- 官方音色库更新后，需要 catalog version 和 hash 共同控制重算。
- `low` 情绪如果 provider 不直接支持，应通过 emotion params 和 instruction 模拟，不能混同为 whisper。

## 20. 完成标准

第一阶段完成标准：

- 可以加载一个 fake voice catalog。
- `voice_select` 能根据 fake catalog 给角色绑定 `voice_type`。
- `role_voice_generation` 实际使用该 `voice_type`。
- `--episodes` 只更新目标 episode 的角色。
- 手工覆盖优先于 Omni 选择。

完整完成标准：

- 全局 Seed TTS catalog 能生成多情绪样例。
- Omni 能为每个音色生成听感画像。
- `voice_select` 能先用画像筛 top 5，再听音频终选。
- 输出中人类可读字段展示中文 `voice_label`。
- API payload 使用官方 `voice_type`。
- 重跑不重复生成已有 catalog 样例和未变化的角色选择。
