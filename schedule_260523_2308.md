# AutoDrama 状态落盘与 Metadata 收敛规划

时间：2026-05-23 23:08  
范围：上一轮角色状态落盘重构结果 + 本轮 `metadata`、`dynamic_assets`、`script_compress` 代码审查结论  
原则：`state.json` 只保留项目运行状态和轻量引用；动态增长内容、节点输出、资产索引落到独立 JSON。新流程不为旧状态格式做兼容。

## 1. 已完成：角色状态落盘重构

上一轮已将角色信息从 `state.json.roles` 的完整对象存储，改为人物原名到角色 JSON 路径的引用表。

当前目标结构：

```json
{
  "roles": {
    "韩默": "assets/json/roles/role_韩默.json"
  }
}
```

运行时仍然 hydrate 成：

```python
state.roles["role_韩默"] = Role(...)
```

这样下游仍可继续使用原来的内存结构，不需要把所有调用点改成读路径。

### 1.1 已完成改动点

- `ProjectRepository.load_state()`
  - 从 `state.json.roles` 读取 `人物原名 -> role_json_path`。
  - 打开每个角色 JSON。
  - 如果 `state_role` 存在，则 hydrate 成运行态 `Role` 并放回 `state.roles[role_id]`。
  - 校验 `state.json.roles` 的键名、角色 JSON 内 `role_name`、`state_role.name` 必须一致，避免路径指错人物。

- `ProjectRepository.save_state()`
  - 写盘时不再把完整 `Role` 对象写进 `state.json.roles`。
  - `state.json.roles` 只写人物原名到角色 JSON 路径。
  - 同步刷新角色 JSON 内的 `state_role` 和 `bound_props`，确保后续图片、音频、视频等资产状态不会只停留在内存。
  - 运行时临时使用的 `metadata["role_refs"]` 不落入 `state.json.metadata`。

- `RoleDesignRepository`
  - 新增 per-role record 格式。
  - `role_extract` 后写：
    ```json
    {
      "schema_version": 1,
      "role_id": "role_韩默",
      "role_name": "韩默",
      "extract": {},
      "design": null,
      "state_role": null,
      "bound_props": [],
      "source": {
        "role_extract_path": "assets/json/nodes/role_extract.json",
        "role_design_path": null
      }
    }
    ```
  - `role_design` 后更新同一个文件，补齐：
    - `design`
    - `state_role`
    - `bound_props`
    - `source.role_design_path`

- `RoleExtractNode`
  - `role_extract` 后为每个人物生成独立角色 JSON。
  - `state.metadata["role_refs"]` 仅作为运行时中间引用，最终由 `save_state()` 写到顶层 `state.json.roles`。

- `RoleDesignNode`
  - 设计结果写入同一个角色 JSON。
  - 复用已有设计时，尽量保留 hydrate 进来的角色资产状态。
  - 保留上一轮加的角色身份校验，避免 `韩默` 设计结果混入 `周瑾` 并被错误 relabel。

### 1.2 已验证

已通过非 pytest 验证：

```powershell
D:/miniforge3/envs/autodrama/python.exe -m compileall autodrama/src scripts/smoke
D:/miniforge3/envs/autodrama/python.exe scripts/smoke/role_refs_state_smoke.py
D:/miniforge3/envs/autodrama/python.exe scripts/smoke/role_design_identity_guard_smoke.py
```

验证覆盖：

- `role_extract` 后 `state.json.roles` 是人物原名到路径。
- `role_韩默.json.extract` 存在，`design/state_role` 为空。
- `role_design` 后同文件更新出 `design/state_role/bound_props`。
- `repo.load_state()` 能从角色 JSON 恢复运行态 `Role`。
- 后续 `repo.save_state()` 会把资产状态重新写回角色 JSON。

## 2. 本轮审查结论：metadata 当前膨胀点

本轮只审查代码，没有改业务逻辑。

### 2.1 可接受的轻量 metadata

这些是常量级或小列表，暂时可以保留：

- `created_by`
- `source_script_file`
- `config_path`
- `episode_count`
- `episode_duration_seconds`
- `bgm_count`
- `visual_style`
- `visual_style_label`
- `visual_style_prompt`
- `script_novel_target_char_count`
- `script_novel_full_episode_paths`
- `script_novel_extract_batch_size`
- `role_design_generation_mode`
- `role_design_active_episode_keys`
- `role_design_target_role_names`
- `role_design_designed_role_names`
- `prop_design_generation_mode`
- `prop_design_active_episode_keys`
- `prop_design_target_prop_names`
- `prop_design_designed_prop_names`
- `edit_plans`
- `final_videos`

其中 `visual_style_prompt` 可能较长，尤其 `cg_animation`，但它是配置常量，不随镜头、角色、资产数量持续增长。

### 2.2 明显应该移出 metadata 的内容

#### `metadata["role_extract"]`

当前位置：

```python
state.metadata["role_extract"] = output.model_dump(mode="json")
```

问题：

- 完整角色提取结果已经落盘在 `assets/json/nodes/role_extract.json`。
- 每个角色 JSON 里也已经有 `extract` 字段。
- 继续放 metadata 属于重复存储。

建议：

- 删除 `metadata["role_extract"]`。
- 如确实需要状态引用，最多保留固定路径字段：
  ```json
  "role_extract_path": "assets/json/nodes/role_extract.json"
  ```
- 更推荐不放 metadata，代码通过固定 layout 找节点输出。

#### `metadata["prop_extract"]`

当前位置：

```python
state.metadata["prop_extract"] = output.model_dump(mode="json")
```

问题：

- 完整道具提取结果已经落盘在 `assets/json/nodes/prop_extract.json`。
- 每个道具 JSON 也保存了 extract/design 信息。
- 继续放 metadata 属于重复存储。

建议：

- 删除 `metadata["prop_extract"]`。
- 如确实需要引用，最多保留固定路径字段：
  ```json
  "prop_extract_path": "assets/json/nodes/prop_extract.json"
  ```
- 更推荐不放 metadata，代码通过固定 layout 找节点输出。

#### `metadata["dynamic_assets"]`

当前位置：

```python
state.metadata["dynamic_assets"] = [...]
```

写入点：

- `workflows/dynamic_assets.py`
- `workflows/generation.py`

问题：

- 这是动态增长资产索引。
- 会随集数、镜头数、参考帧、镜头视频、台词音频、BGM 资产数量持续增长。
- 不应该放在 `state.json.metadata`。

结论：

- 所有动态增长资产索引必须归到单独 JSON。
- `state.json.metadata` 不再保存 `dynamic_assets` 列表。

建议目标文件：

```text
assets/json/assets/dynamic_assets.json
```

建议结构：

```json
{
  "schema_version": 1,
  "assets": [
    {
      "asset_id": "shot_001_video",
      "asset_type": "shot_video",
      "episode_key": "episode_001",
      "shot_id": "shot_001",
      "asset_path": "assets/videos/shots/shot_001.mp4",
      "source_node": "shot_video_generation",
      "reuse_scope": "shot",
      "metadata": {}
    }
  ]
}
```

是否在 metadata 放路径：

- 可选方案 A：`metadata["dynamic_assets_path"] = "assets/json/assets/dynamic_assets.json"`。
- 可选方案 B：不放路径，代码通过固定 layout 读取。

推荐方案 B。路径属于项目固定布局，不需要重复写到 state。

注意：

- 现有 `assets/json/nodes/dynamic_asset_solidification.json` 是节点输出，不等价于完整累计索引。
- 迁移时需要新增“累计索引 JSON”的读写逻辑，而不是只依赖节点输出。

## 3. script_compress 审查结论

### 3.1 当前不是死代码

`script_compress` 现在仍在 pregen 主流程中：

```python
STATIC_ASSET_NODE_NAMES = [
    "role_appearance_generation",
    "prop_extract",
    "prop_design",
    "prop_generation",
    "script_compress",
    "layout_design",
    "layout_dedupe_review",
    "layout_image_generation",
]
```

当前节点会写：

```python
state.metadata["simple_script"] = output.simple_script
state.metadata["global_script"] = output.global_script
```

下游仍读取：

- `layout_design`
  - `simple_script`
  - `global_script`

- `bgm_design`
  - `simple_script`
  - `global_script`

对应 prompt 文件也仍包含这两个输入：

- `prompts/layout_design.md`
- `prompts/bgm_design.md`

所以当前代码层面，`script_compress` 不是未引用代码。

### 3.2 但它已经可以废弃

现在已经有 `script_novel_extract`，后续资产规划节点的 `episode_stories()` 已经优先读取：

```python
state.script.novel_extract
```

如果没有 `novel_extract`，才回退到：

```python
state.script.novel_full
```

因此：

- `layout_design` 已经能拿到 `novel_extract`。
- `bgm_design` 已经能拿到 `novel_extract`。
- `script_compress` 是额外再压缩一次，职责和 `novel_extract` 重叠。
- 它还增加一次 LLM 调用，并把 `simple_script/global_script` 塞进 metadata。

结论：

- `script_compress` 当前不是死代码。
- 但从新流程设计看，应删除。
- 后续 `layout_design` 和 `bgm_design` 应直接依赖 `novel_extract` 作为剧情依据。

## 4. 后续执行计划

### Phase 1：收紧 metadata，不改大流程

目标：先去掉明显重复存储，降低 `state.json.metadata` 膨胀。

任务：

1. 删除 `state.metadata["role_extract"]` 写入。
2. 删除 `state.metadata["prop_extract"]` 写入。
3. 确认没有下游读取这两个字段。
4. 补 smoke：
   - 跑 `role_extract` 后，确认 `state.json.metadata` 不含 `role_extract`。
   - 跑 `prop_extract` 后，确认 `state.json.metadata` 不含 `prop_extract`。
   - 确认节点输出 JSON 和 per-role/per-prop JSON 仍存在。

预期结果：

- `state.json.metadata` 不再重复保存完整 extract 输出。
- 数据来源统一到节点输出和分项 JSON。

### Phase 2：dynamic_assets 独立索引

目标：所有动态增长资产索引移出 `state.json.metadata`。

任务：

1. 在 `ProjectLayout` 增加动态资产索引路径方法，例如：
   ```python
   dynamic_assets_index_path(project_dir)
   ```
   目标路径：
   ```text
   assets/json/assets/dynamic_assets.json
   ```

2. 新增 repository/helper，例如：
   ```python
   DynamicAssetRepository
   ```

3. 支持操作：
   - `load_index(project_dir) -> list[DynamicAssetSolidificationItem]`
   - `save_index(project_dir, items)`
   - `merge_episode_assets(project_dir, episode_key, new_items)`
   - `merge_assets(project_dir, new_items, replace_keys=...)`

4. 修改写入点：
   - `workflows/dynamic_assets.py`
   - `workflows/generation.py`

5. 删除：
   ```python
   state.metadata["dynamic_assets"]
   ```

6. 如有读取点，改成读独立 JSON。

7. 补 smoke：
   - 单集生成后写入 `assets/json/assets/dynamic_assets.json`。
   - 重跑某一集时只替换该集资产，不复制旧资产。
   - `state.json.metadata` 不含 `dynamic_assets`。
   - 节点输出 `assets/json/nodes/dynamic_asset_solidification.json` 仍保留本次节点结果。

预期结果：

- `state.json` 不再随镜头和动态资产数量膨胀。
- 动态资产索引有独立、稳定、可累计的 JSON。

### Phase 3：删除 script_compress

目标：去掉和 `novel_extract` 职责重叠的压缩节点，移除 `simple_script/global_script` metadata。

任务：

1. 从 `STATIC_ASSET_NODE_NAMES` 删除：
   ```python
   "script_compress"
   ```

2. 删除或停用：
   - `ScriptCompressNode`
   - `PregenWorkflow._run_script_compress()`
   - `AssetService.script_compress()`
   - `prompts/script_compress.md`
   - `ScriptCompressOutput`
   - fake provider 中的 `script_compress` 分支

3. 修改 `AssetService.layout_design()`：
   - 删除 `simple_script` 参数。
   - 删除 `global_script` 参数。
   - 只传 `episode_stories`、角色、道具、画风。

4. 修改 `AssetService.bgm_design()`：
   - 删除 `simple_script` 参数。
   - 删除 `global_script` 参数。
   - 只传 `episode_stories`、画风、BGM 数量。

5. 修改 prompt：
   - `layout_design.md`
     - 删除“简约分集剧本”
     - 删除“全局简约剧本”
     - 明确“分集剧情依据来自 novel_extract”
   - `bgm_design.md`
     - 删除“简约分集剧本”
     - 删除“全局简约剧本”
     - 明确根据“分集剧情依据”设计 BGM

6. 删除 metadata 写入：
   ```python
   state.metadata["simple_script"]
   state.metadata["global_script"]
   ```

7. 检查 `PREGEN_NODE_NAMES`、CLI `until/only` 是否还接受 `script_compress`。
   - 新流程不做历史兼容。
   - 用户如果传 `--only script_compress`，应直接报 unsupported。

8. 更新 smoke：
   - 删除或改造依赖 `script_compress` 的 smoke。
   - 增加验证：跑到 `layout_design` 时不需要 `simple_script/global_script`。
   - 增加验证：`state.json.metadata` 不含 `simple_script/global_script`。

预期结果：

- pregen 少一次 LLM 调用。
- `state.json.metadata` 不再保存简约剧本正文。
- 后续资产规划统一依赖 `script_novel_extract`。

### Phase 4：ProjectMetadata 清理

当前 `core/metadata.py` 中 `ProjectMetadata` 仍声明：

```python
simple_script
global_script
dynamic_assets
```

在 Phase 2 和 Phase 3 完成后，需要同步清理。

任务：

1. 从 `ProjectMetadata` 删除：
   - `simple_script`
   - `global_script`
   - `dynamic_assets`

2. 如果仍需要读取动态资产，改为通过新 repository 读取独立索引 JSON。

3. 确认 `from_state_metadata()` 不再把动态增长字段视为 known metadata。

预期结果：

- metadata schema 和新的轻量 state 原则一致。

## 5. 推荐最终 state.json 形态

目标方向：

```json
{
  "project_id": "xcj-2",
  "title": "...",
  "raw_script": "...",
  "current_node": "bgm_generation",
  "completed_nodes": [],
  "script": {
    "raw_script": "...",
    "episode_outlines": {
      "episode_001": "assets/json/scripts/outlines/episode_001.json"
    },
    "novel_full": {
      "episode_001": "assets/json/scripts/novel_full/episode_001.json"
    },
    "novel_extract": {
      "episode_001": "assets/json/scripts/novel_extract/episode_001.json"
    }
  },
  "roles": {
    "韩默": "assets/json/roles/role_韩默.json"
  },
  "props": {
    "prop_短匕": {
      "id": "prop_短匕",
      "name": "短匕",
      "design_path": "assets/json/props/prop_短匕.json",
      "asset_path": "assets/images/props/prop_短匕.png"
    }
  },
  "layouts": {},
  "bgms": {},
  "budget": {},
  "metadata": {
    "created_by": "autodrama",
    "source_script_file": "...",
    "config_path": "...",
    "episode_count": 1,
    "episode_duration_seconds": 30,
    "bgm_count": 3,
    "visual_style": "cg_animation",
    "visual_style_label": "爱死机写实CG风",
    "visual_style_prompt": "..."
  },
  "created_at": "...",
  "updated_at": "..."
}
```

不应再出现在 `state.json.metadata` 中：

- `role_extract`
- `prop_extract`
- `dynamic_assets`
- `simple_script`
- `global_script`

## 6. 验证约束

仓库约束：

- 不使用 pytest。
- 不运行 `pytest` 或 `python -m pytest`。
- 不使用 `python -`、heredoc 或控制台喂脚本做验证。
- agent 自写 smoke 放在：
  ```text
  scripts/smoke/
  ```
- smoke 临时输出放在：
  ```text
  .tmp/
  ```

推荐验证命令：

```powershell
D:/miniforge3/envs/autodrama/python.exe -m compileall autodrama/src scripts/smoke
D:/miniforge3/envs/autodrama/python.exe scripts/smoke/role_refs_state_smoke.py
D:/miniforge3/envs/autodrama/python.exe scripts/smoke/role_design_identity_guard_smoke.py
```

后续新增 smoke 后，继续用显式脚本路径运行。

## 7. 建议执行顺序

1. 先做 Phase 1。
   - 风险低。
   - 立刻减少重复 metadata。

2. 再做 Phase 2。
   - 需要新增独立动态资产索引。
   - 是控制 `state.json` 长期膨胀的关键。

3. 最后做 Phase 3。
   - 会影响 pregen 节点列表、prompt、service、fake provider 和 smoke。
   - 虽然方向明确，但改动面比 Phase 1/2 更宽。

4. Phase 4 跟随 Phase 2/3 收尾。
   - 清理 schema 和 metadata helper。

## 8. 当前状态判断

- 角色状态落盘重构已经完成并通过 smoke。
- `metadata["role_refs"]` 只是运行时辅助，不落盘到 `state.json.metadata`。
- `metadata["role_extract"]` 和 `metadata["prop_extract"]` 是明显重复存储，应优先删。
- `metadata["dynamic_assets"]` 是动态增长资产索引，应迁移到独立 JSON。
- `script_compress` 当前仍被流程和 prompt 使用，但已经可由 `novel_extract` 替代，建议按计划删除。
